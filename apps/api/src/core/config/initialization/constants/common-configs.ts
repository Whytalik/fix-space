export const DATE_CONFIG = {
  defaultValue: null,
  format: "DD.MM.YYYY",
  includeTime: false,
  timeFormat: "HH:mm",
} as const;

export const FORMULA_TEXT = { formula: "", output: { type: "text" } };
