version: '3.8'

volumes:
  database:
    driver: local

services:
  db:
    platform: linux/amd64
    image: postgres:15
    restart: always
    env_file:
      - .env
    ports:
      - '5432:5432'
    volumes:
      - database:/var/lib/postgresql/data
