## 3. Sections (TC-SEC-001 — TC-SEC-014)

---

### TC-SEC-001: Створення розділу

| Поле         | Значення                         |
| ------------ | -------------------------------- |
| **US**       | US-018                           |
| **Issue**    | #38                              |
| **TS**       | TS-SPACE-01                      |
| **Метод**    | Black Box (Postman)              |
| **Техніка**  | Equivalence Partitioning (valid) |
| **Priority** | P1                               |

**Кроки:**

1. Увійти в систему, отримати spaceId
2. `POST /sections` з тілом:
   ```json
   { "spaceId": "<space-id>", "name": "Trading", "icon": "📊", "color": "blue" }
   ```
3. Перевірити статус-код та тіло відповіді

**Очікуваний результат:**

- Статус: `201 Created`
- Body: секція з `name: "Trading"`, `icon: "📊"`, `color: "blue"`

---

### TC-SEC-002: Створення розділу з порожньою назвою

| Поле         | Значення                           |
| ------------ | ---------------------------------- |
| **US**       | US-018                             |
| **Issue**    | #38                                |
| **TS**       | TS-SPACE-01                        |
| **Метод**    | Black Box (Postman)                |
| **Техніка**  | Equivalence Partitioning (invalid) |
| **Priority** | P1                                 |

**Кроки:**

1. `POST /sections` з тілом: `{ "spaceId": "<space-id>", "name": "" }`
2. Перевірити статус-код

**Очікуваний результат:**

- Статус: `400 Bad Request`
- Body містить помилку валідації поля `name`

---

### TC-SEC-003: Створення розділу з дублікатом назви

| Поле         | Значення                             |
| ------------ | ------------------------------------ |
| **US**       | US-018                               |
| **Issue**    | #38                                  |
| **TS**       | TS-SPACE-01                          |
| **Метод**    | Black Box (Postman)                  |
| **Техніка**  | Equivalence Partitioning (duplicate) |
| **Priority** | P1                                   |

**Кроки:**

1. Створити секцію "Trading"
2. `POST /sections` з тілом: `{ "spaceId": "<space-id>", "name": "Trading" }`
3. Перевірити статус-код

**Очікуваний результат:**

- Статус: `409 Conflict`
- Body: повідомлення про дублікат назви в межах простору

---

### TC-SEC-004: Перейменування розділу

| Поле         | Значення                 |
| ------------ | ------------------------ |
| **US**       | US-019                   |
| **Issue**    | #39                      |
| **TS**       | TS-SPACE-01              |
| **Метод**    | Black Box (Postman)      |
| **Техніка**  | Equivalence Partitioning |
| **Priority** | P1                       |

**Кроки:**

1. Створити секцію "Trading"
2. `PATCH /sections/:id` з тілом: `{ "name": "Active Trading" }`
3. `GET /sections/:id` — перевірити нову назву

**Очікуваний результат:**

- Статус: `200 OK`
- Назва оновлена на "Active Trading"

---

### TC-SEC-005: Перейменування з порожньою назвою

| Поле         | Значення            |
| ------------ | ------------------- |
| **US**       | US-019              |
| **Issue**    | #39                 |
| **TS**       | TS-SPACE-01         |
| **Метод**    | Black Box (Postman) |
| **Техніка**  | Error Guessing      |
| **Priority** | P1                  |

**Кроки:**

1. `PATCH /sections/:id` з тілом: `{ "name": "" }`
2. Перевірити статус-код

**Очікуваний результат:**

- Статус: `400 Bad Request`
- Назва НЕ змінена

---

### TC-SEC-006: Видалення порожнього розділу

| Поле         | Значення            |
| ------------ | ------------------- |
| **US**       | US-020              |
| **Issue**    | #40                 |
| **TS**       | TS-SPACE-01         |
| **Метод**    | Black Box (Postman) |
| **Техніка**  | State Transition    |
| **Priority** | P1                  |

**Кроки:**

1. Створити порожню секцію (без БД)
2. `DELETE /sections/:id`
3. `GET /sections/:id` — перевірити що не існує

**Очікуваний результат:**

- Статус: `200 OK`
- Секція видалена, `GET` повертає `404`

---

### TC-SEC-007: Видалення розділу з базами даних

| Поле         | Значення            |
| ------------ | ------------------- |
| **US**       | US-020              |
| **Issue**    | #40                 |
| **TS**       | TS-SPACE-01         |
| **Метод**    | Black Box (Postman) |
| **Техніка**  | State Transition    |
| **Priority** | P1                  |

**Кроки:**

1. Створити секцію, додати в неї БД
2. `DELETE /sections/:id`
3. Перевірити що БД НЕ видалені, `sectionId` став `null`

**Очікуваний результат:**

- Статус: `200 OK`
- БД залишилися в просторі (sectionId = null), дані не втрачені

---

### TC-SEC-008: Видалення чужого розділу

| Поле         | Значення            |
| ------------ | ------------------- |
| **US**       | US-020              |
| **Issue**    | #40                 |
| **TS**       | TS-SPACE-01         |
| **Метод**    | Black Box (Postman) |
| **Техніка**  | Security Test       |
| **Priority** | P1                  |

**Кроки:**

1. Користувач A створює секцію
2. Користувач B намагається `DELETE /sections/:id`
3. Перевірити статус-код

**Очікуваний результат:**

- Статус: `403 Forbidden`
- Секція не видалена

---

### TC-SEC-009: Зміна порядку розділів

| Поле         | Значення                 |
| ------------ | ------------------------ |
| **US**       | US-021                   |
| **Issue**    | #41                      |
| **TS**       | TS-SPACE-01              |
| **Метод**    | Black Box (Postman)      |
| **Техніка**  | Equivalence Partitioning |
| **Priority** | P1                       |

**Кроки:**

1. Створити 3 секції
2. `PATCH /sections/:id` з тілом: `{ "order": 0 }` (перемістити на перше місце)
3. `GET /sections?spaceId=<space-id>` — перевірити порядок

**Очікуваний результат:**

- Статус: `200 OK`
- Секція з `order: 0` перша у списку

---

### TC-SEC-010: Зміна порядку — збереження між сесіями

| Поле         | Значення            |
| ------------ | ------------------- |
| **US**       | US-021              |
| **Issue**    | #41                 |
| **TS**       | TS-SPACE-01         |
| **Метод**    | Black Box (Postman) |
| **Техніка**  | State Transition    |
| **Priority** | P1                  |

**Кроки:**

1. Змінити порядок секцій (TC-SEC-009)
2. Вийти та увійти знову
3. `GET /sections` — перевірити порядок

**Очікуваний результат:**

- Порядок зберігся після повторного входу

---

### TC-SEC-011: Згортання розділу

| Поле         | Значення            |
| ------------ | ------------------- |
| **US**       | US-022              |
| **Issue**    | #74                 |
| **TS**       | —                   |
| **Метод**    | Manual (Browser)    |
| **Техніка**  | Exploratory Testing |
| **Priority** | P2                  |

**Кроки:**

1. Відкрити sidebar з розділами
2. Клікнути на стрілку згортання розділу
3. Перевірити що БД приховані
4. Оновити сторінку — перевірити стан

**Очікуваний результат:**

- Розділ згорнутий, БД приховані
- Стан згорнутості зберігся після оновлення

---

### TC-SEC-012: Розгортання розділу

| Поле         | Значення            |
| ------------ | ------------------- |
| **US**       | US-022              |
| **Issue**    | #74                 |
| **TS**       | —                   |
| **Метод**    | Manual (Browser)    |
| **Техніка**  | Exploratory Testing |
| **Priority** | P2                  |

**Кроки:**

1. Згорнути розділ (TC-SEC-011)
2. Клікнути на стрілку розгортання
3. Перевірити що БД відображаються

**Очікуваний результат:**

- Розділ розгорнутий, всі БД видимі

---

### TC-SEC-013: Переміщення БД між розділами

| Поле         | Значення                 |
| ------------ | ------------------------ |
| **US**       | US-023                   |
| **Issue**    | #75                      |
| **TS**       | —                        |
| **Метод**    | Black Box (Postman)      |
| **Техніка**  | Equivalence Partitioning |
| **Priority** | P2                       |

**Кроки:**

1. Створити дві секції: "Trading" та "Analytics"
2. Створити БД в "Trading"
3. `PATCH /databases/:id` з тілом: `{ "sectionId": "<analytics-section-id>" }`
4. Перевірити що БД тепер в "Analytics"

**Очікуваний результат:**

- Статус: `200 OK`
- БД відображається в розділі "Analytics", дані не втрачені

---

### TC-SEC-014: Переміщення БД — зв'язки збережені

| Поле         | Значення            |
| ------------ | ------------------- |
| **US**       | US-023              |
| **Issue**    | #75                 |
| **TS**       | —                   |
| **Метод**    | Black Box (Postman) |
| **Техніка**  | State Transition    |
| **Priority** | P2                  |

**Кроки:**

1. Перемістити БД між розділами (TC-SEC-013)
2. Перевірити RELATION-властивості в інших БД що посилаються на переміщену
3. Перевірити що записи доступні

**Очікуваний результат:**

- RELATION-зв'язки працюють коректно
- Записи доступні, дані цілі

---

### TC-SEC-015: Створення та відображення нової секції в боковому меню (Playwright UI E2E)

| Поле         | Значення                              |
| ------------ | ------------------------------------- |
| **US**       | US-018                                |
| **Issue**    | #38                                   |
| **TS**       | TS-SPACE-01                           |
| **Метод**    | E2E UI (Playwright)                   |
| **Техніка**  | Equivalence Partitioning (valid path) |
| **Priority** | P1                                    |

**Кроки:**

1. Увійти в систему
2. У лівому меню (Sidebar) натиснути на кнопку "+ Додати секцію" (Add Section)
3. У модальному вікні ввести назву секції `"Аналітика та стратегії"`
4. Вибрати іконку `"📈"` та колір `"blue"`
5. Натиснути "Зберегти"
6. Перевірити, що нова секція відображається в боковому меню з правильним текстом та іконкою

**Очікуваний результат:**

- Секція створюється в базі даних та відображається в боковій панелі
- Всі іконки та назва секції рендеряться миттєво без потреби перезавантаження

---

### TC-SEC-016: Перейменування секції через UI (Playwright)

| Поле         | Значення                              |
| ------------ | ------------------------------------- |
| **US**       | US-019                                |
| **Issue**    | —                                     |
| **TS**       | —                                     |
| **Метод**    | E2E UI (Playwright)                   |
| **Техніка**  | Equivalence Partitioning (valid path) |
| **Priority** | P2                                    |

**Кроки:**

1. Увійти в систему та відкрити робочий простір зі створеною секцією
2. Двічі клікнути на назву секції в боковому меню або вибрати "Rename" з контекстного меню
3. Очистити поточну назву та ввести нову `"Перейменована секція"`
4. Підтвердити зміну клавішею Enter або кліком поза полем введення
5. Перевірити оновлену назву в боковій панелі

**Очікуваний результат:**

- Назва секції миттєво оновлюється в боковому меню без перезавантаження сторінки
- Нова назва зберігається та відображається коректно після оновлення сторінки

---
