# Unit Tests: Sections

### [ ] TC-SEC-U-001: SectionService.create — Створення секції з правильними параметрами

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-022             |
| **Issue**    | #38                |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати `SectionService.create(spaceId, { name: "Analytics", position: 1, icon: "📈", color: "blue" })`
2. Перевірити, що `SectionRepository.create` викликається з правильними параметрами
3. Перевірити повернення `SectionResponseDto`

**Очікуваний результат:**

- Репозиторій отримує правильні дані секції
- Повертається об'єкт `SectionResponseDto` із заповненими полями

---

---

### [ ] TC-SEC-U-002: SectionService.processOperations (CREATE) — Обчислення позиції секції за замовчуванням

| Поле         | Значення        |
| ------------ | --------------- |
| **US**       | US-022          |
| **Issue**    | #38             |
| **TS**       | —               |
| **Метод**    | Unit (Jest)     |
| **Техніка**  | Branch Coverage |
| **Priority** | P1              |

**Кроки:**

1. Викликати `processOperations` з операцією типу `CREATE`, де `position` не вказано
2. Налаштувати репозиторій на повернення `findLastPosition` з позицією `3`
3. Перевірити, що для нової секції встановлюється `position: 4`

**Очікуваний результат:**

- Позиція нової секції встановлюється як `lastPosition + 1` (тобто `4`)

---

---

### [ ] TC-SEC-U-003: SectionService.processOperations (UPDATE) — Валідація унікальності назви секції

| Поле         | Значення        |
| ------------ | --------------- |
| **US**       | US-022          |
| **Issue**    | #39             |
| **TS**       | —               |
| **Метод**    | Unit (Jest)     |
| **Техніка**  | Branch Coverage |
| **Priority** | P1              |

**Кроки:**

1. Викликати `processOperations` з операцією типу `UPDATE` для перейменування секції на `"Trading"`
2. Налаштувати `SectionRepository.findDuplicate` на повернення іншої існуючої секції з такою ж назвою у цьому просторі
3. Перевірити викидання `BadRequestException`

**Очікуваний результат:**

- Викидається `BadRequestException` з помилкою про зайняту назву секції (`errors.SECTION_NAME_TAKEN`)

---

---

### [ ] TC-SEC-U-004: SectionService.processOperations (DELETE) — Видалення секції та ізоляція від простору

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-022             |
| **Issue**    | #40                |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Налаштувати репозиторій для повернення секції з відповідним `spaceId`
2. Викликати `processOperations` з операцією `DELETE` та валідним `id`
3. Перевірити, що викликається `SectionRepository.delete`

**Очікуваний результат:**

- Репозиторій успішно виконує видалення секції за її ідентифікатором

---

---

### [ ] TC-SEC-U-005: SectionService.processOperations — Валідація вхідних даних операцій

| Поле         | Значення       |
| ------------ | -------------- |
| **US**       | US-022, US-022 |
| **Issue**    | #38, #39       |
| **TS**       | —              |
| **Метод**    | Unit (Jest)    |
| **Техніка**  | Boundary Value |
| **Priority** | P1             |

**Кроки:**

1. Викликати `processOperations` з операцією `CREATE`, але без поля `create`
2. Викликати `processOperations` з операцією `UPDATE` без поля `id`
3. Викликати `processOperations` з операцією `UPDATE`, де секція належить іншому простору
4. Перевірити викидання `BadRequestException` для кожного випадку

**Очікуваний результат:**

- Всі невалідні операції перериваються викиданням `BadRequestException`

---

---

---

### [ ] TC-SEC-U-006: SectionService — Відкат транзакції при виникненні помилки

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-022             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Налаштувати мок репозиторію або Prisma клієнта на успішне виконання першої частини транзакції (наприклад, створення запису).
2. Налаштувати мок репозиторію так, щоб наступний крок транзакції або супутня дія викликали виняток/помилку.
3. Викликати відповідний метод сервісу/usecase в транзакції.
4. Перевірити, що всі зміни, виконані в межах транзакції, відкочуються і помилка прокидається вище.

**Очікуваний результат:**

- Транзакція відміняється (rollback).
- Жодні проміжні зміни не зберігаються в базі даних.
- Метод прокидає відповідну помилку.

---

---

### [ ] TC-SEC-U-007: SectionService — Обмеження доступу до ресурсу, який не належить користувачу

| Поле         | Значення        |
| ------------ | --------------- |
| **US**       | US-022          |
| **Issue**    | —               |
| **TS**       | —               |
| **Метод**    | Unit (Jest)     |
| **Техніка**  | Branch Coverage |
| **Priority** | P1              |

**Кроки:**

1. Налаштувати мок репозиторію для повернення ресурсу, де `ownerId` відрізняється від ID поточного користувача.
2. Викликати метод сервісу для доступу або модифікації цього ресурсу від імені поточного користувача.
3. Перевірити, що метод викидає `ForbiddenException`.

**Очікуваний результат:**

- Метод повертає `ForbiddenException` та не виконує операцію.
