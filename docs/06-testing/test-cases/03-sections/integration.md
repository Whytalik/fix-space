# Integration Tests: Sections

### [x] TC-SEC-001: Створення розділу

| Поле         | Значення                         |
| ------------ | -------------------------------- |
| **US**       | US-022                           |
| **Issue**    | #38                              |
| **TS**       | TS-SPACE-01                      |
| **Метод**    | Integration (Supertest)          |
| **Техніка**  | Equivalence Partitioning (valid) |
| **Priority** | P1                               |

**Кроки:**

1. Увійти в систему, отримати spaceId
2. `POST /sections` з тілом:
   ```json
   { "spaceId": "<space-id>", "name": "Trading", "icon": "📊", "color": "blue" }
   ```
3. Перевірити статус-код та тіло відповіді

**Очікуваний результат:**

- Статус: `201 Created`
- Body: секція з `name: "Trading"`, `icon: "📊"`, `color: "blue"`

---

### [x] TC-SEC-002: Створення розділу з порожньою назвою

| Поле         | Значення                           |
| ------------ | ---------------------------------- |
| **US**       | US-022                             |
| **Issue**    | #38                                |
| **TS**       | TS-SPACE-01                        |
| **Метод**    | Integration (Supertest)            |
| **Техніка**  | Equivalence Partitioning (invalid) |
| **Priority** | P1                                 |

**Кроки:**

1. `POST /sections` з тілом: `{ "spaceId": "<space-id>", "name": "" }`
2. Перевірити статус-код

**Очікуваний результат:**

- Статус: `400 Bad Request`
- Body містить помилку валідації поля `name`

---

### [x] TC-SEC-003: Створення розділу з дублікатом назви

| Поле         | Значення                             |
| ------------ | ------------------------------------ |
| **US**       | US-022                               |
| **Issue**    | #38                                  |
| **TS**       | TS-SPACE-01                          |
| **Метод**    | Integration (Supertest)              |
| **Техніка**  | Equivalence Partitioning (duplicate) |
| **Priority** | P1                                   |

**Кроки:**

1. Створити секцію "Trading"
2. `POST /sections` з тілом: `{ "spaceId": "<space-id>", "name": "Trading" }`
3. Перевірити статус-код

**Очікуваний результат:**

- Статус: `409 Conflict`
- Body: повідомлення про дублікат назви в межах простору

---

### [x] TC-SEC-004: Перейменування розділу

| Поле         | Значення                 |
| ------------ | ------------------------ |
| **US**       | US-022                   |
| **Issue**    | #39                      |
| **TS**       | TS-SPACE-01              |
| **Метод**    | Integration (Supertest)  |
| **Техніка**  | Equivalence Partitioning |
| **Priority** | P1                       |

**Кроки:**

1. Створити секцію "Trading"
2. `PATCH /sections/:id` з тілом: `{ "name": "Active Trading" }`
3. `GET /sections/:id` — перевірити нову назву

**Очікуваний результат:**

- Статус: `200 OK`
- Назва оновлена на "Active Trading"

---

### [x] TC-SEC-005: Перейменування з порожньою назвою

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-022                  |
| **Issue**    | #39                     |
| **TS**       | TS-SPACE-01             |
| **Метод**    | Integration (Supertest) |
| **Техніка**  | Error Guessing          |
| **Priority** | P1                      |

**Кроки:**

1. `PATCH /sections/:id` з тілом: `{ "name": "" }`
2. Перевірити статус-код

**Очікуваний результат:**

- Статус: `400 Bad Request`
- Назва НЕ змінена

---

### [x] TC-SEC-006: Видалення порожнього розділу

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-022                  |
| **Issue**    | #40                     |
| **TS**       | TS-SPACE-01             |
| **Метод**    | Integration (Supertest) |
| **Техніка**  | State Transition        |
| **Priority** | P1                      |

**Кроки:**

1. Створити порожню секцію (без БД)
2. `DELETE /sections/:id`
3. `GET /sections/:id` — перевірити що не існує

**Очікуваний результат:**

- Статус: `200 OK`
- Секція видалена, `GET` повертає `404`

---

### [x] TC-SEC-007: Видалення розділу з базами даних

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-022                  |
| **Issue**    | #40                     |
| **TS**       | TS-SPACE-01             |
| **Метод**    | Integration (Supertest) |
| **Техніка**  | State Transition        |
| **Priority** | P1                      |

**Кроки:**

1. Створити секцію, додати в неї БД
2. `DELETE /sections/:id`
3. Перевірити що БД НЕ видалені, `sectionId` став `null`

**Очікуваний результат:**

- Статус: `200 OK`
- БД залишилися в просторі (sectionId = null), дані не втрачені

---

### [x] TC-SEC-008: Видалення чужого розділу

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-022                  |
| **Issue**    | #40                     |
| **TS**       | TS-SPACE-01             |
| **Метод**    | Integration (Supertest) |
| **Техніка**  | Security Test           |
| **Priority** | P1                      |

**Кроки:**

1. Користувач A створює секцію
2. Користувач B намагається `DELETE /sections/:id`
3. Перевірити статус-код

**Очікуваний результат:**

- Статус: `403 Forbidden`
- Секція не видалена

---

### [x] TC-SEC-009: Зміна порядку розділів

| Поле         | Значення                 |
| ------------ | ------------------------ |
| **US**       | US-022                   |
| **Issue**    | #41                      |
| **TS**       | TS-SPACE-01              |
| **Метод**    | Integration (Supertest)  |
| **Техніка**  | Equivalence Partitioning |
| **Priority** | P1                       |

**Кроки:**

1. Створити 3 секції
2. `PATCH /sections/:id` з тілом: `{ "order": 0 }` (перемістити на перше місце)
3. `GET /sections?spaceId=<space-id>` — перевірити порядок

**Очікуваний результат:**

- Статус: `200 OK`
- Секція з `order: 0` перша у списку

---

### [x] TC-SEC-010: Зміна порядку — збереження між сесіями

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-022                  |
| **Issue**    | #41                     |
| **TS**       | TS-SPACE-01             |
| **Метод**    | Integration (Supertest) |
| **Техніка**  | State Transition        |
| **Priority** | P1                      |

**Кроки:**

1. Змінити порядок секцій (TC-SEC-009)
2. Вийти та увійти знову
3. `GET /sections` — перевірити порядок

**Очікуваний результат:**

- Порядок зберігся після повторного входу

---

### [x] TC-SEC-013: Переміщення БД між розділами

| Поле         | Значення                 |
| ------------ | ------------------------ |
| **US**       | US-051                   |
| **Issue**    | #75                      |
| **TS**       | —                        |
| **Метод**    | Integration (Supertest)  |
| **Техніка**  | Equivalence Partitioning |
| **Priority** | P2                       |

**Кроки:**

1. Створити дві секції: "Trading" та "Analytics"
2. Створити БД в "Trading"
3. `PATCH /databases/:id` з тілом: `{ "sectionId": "<analytics-section-id>" }`
4. Перевірити що БД тепер в "Analytics"

**Очікуваний результат:**

- Статус: `200 OK`
- БД відображається в розділі "Analytics", дані не втрачені

---

### [x] TC-SEC-014: Переміщення БД — зв'язки збережені

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-051                  |
| **Issue**    | #75                     |
| **TS**       | —                       |
| **Метод**    | Integration (Supertest) |
| **Техніка**  | State Transition        |
| **Priority** | P2                      |

**Кроки:**

1. Перемістити БД між розділами (TC-SEC-013)
2. Перевірити RELATION-властивості в інших БД що посилаються на переміщену
3. Перевірити що записи доступні

**Очікуваний результат:**

- RELATION-зв'язки працюють коректно
- Записи доступні, дані цілі

---

### [x] TC-SEC-017: Запит до захищеного ендпоінту без токена автентифікації

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-022                  |
| **Issue**    | —                       |
| **TS**       | —                       |
| **Метод**    | Integration (Supertest) |
| **Техніка**  | Security Testing        |
| **Priority** | P1                      |

**Кроки:**

1. Надіслати запит до будь-якого захищеного ендпоінту модуля Sections (наприклад, `GET /sections`) без заголовка `Authorization`.
2. Перевірити статус-код відповіді та повідомлення про помилку.

**Очікуваний результат:**

- Статус: `401 Unauthorized`
- Body містить повідомлення про відсутність або невалідність токена.

---

### [x] TC-SEC-018: Спроба доступу до неіснуючого ресурсу

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-022                  |
| **Issue**    | —                       |
| **TS**       | —                       |
| **Метод**    | Integration (Supertest) |
| **Техніка**  | Error Guessing          |
| **Priority** | P2                      |

**Кроки:**

1. Авторизуватися в системі.
2. Спробувати надіслати запит для отримання, оновлення або видалення ресурсу модуля Sections з неіснуючим ID (наприклад, `00000000-0000-0000-0000-000000000000` або `999999`).
3. Перевірити статус-код відповіді.

**Очікуваний результат:**

- Статус: `404 Not Found`
- Body містить повідомлення про те, що ресурс не знайдено.
