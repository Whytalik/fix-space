# Unit Tests: Record content

### [ ] TC-CONT-U-001: ContentAreaService.addRow — валідація глибини вкладення рядків

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-053                  |
| **Issue**    | #23                     |
| **TS**       | —                       |
| **Метод**    | Unit (Jest)             |
| **Техніка**  | Boundary Value Analysis |
| **Priority** | P1                      |

**Кроки:**

1. Створити структуру рядків: Рядок А -> Колонка А1 -> Рядок Б (вкладення 1 рівень).
2. Додати до Рядка Б вкладений Рядок В (вкладення 2 рівень).
3. Спробувати додати в Рядок В ще один вкладений Рядок Г (вкладення 3 рівень).

**Очікуваний результат:**

- Додавання Рядка В (глибина 2) проходить успішно.
- Додавання Рядка Г (глибина 3) викликає виключення `BadRequestException` з повідомленням про ліміт вкладеності.

---

---

### [ ] TC-CONT-U-002: ContentAreaService.addColumn — ліміт колонок у рядку (1-5)

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-053                  |
| **Issue**    | #23                     |
| **TS**       | —                       |
| **Метод**    | Unit (Jest)             |
| **Техніка**  | Boundary Value Analysis |
| **Priority** | P1                      |

**Кроки:**

1. Створити порожній Рядок.
2. По черзі додати 5 колонок.
3. Спробувати додати 6-ту колонку.

**Очікуваний результат:**

- Додавання перших 5-ти колонок проходить успішно.
- Спроба додати 6-ту колонку повертає `BadRequestException` через перевищення ліміту у 5 колонок.

---

---

### [ ] TC-CONT-U-003: ContentAreaService.updateBlockStyle — валідація візуальних налаштувань блоку

| Поле         | Значення                 |
| ------------ | ------------------------ |
| **US**       | US-054                   |
| **Issue**    | #24                      |
| **TS**       | —                        |
| **Метод**    | Unit (Jest)              |
| **Техніка**  | Equivalence Partitioning |
| **Priority** | P2                       |

**Кроки:**

1. Викликати оновлення стилю блоку з коректними параметрами (рамка `dashed`, фон `red`, заокруглення `medium`).
2. Викликати оновлення стилю блоку з некоректним значенням фону (`unknown-color`) або непідтримуваною рамкою (`double`).

**Очікуваний результат:**

- Валідні стилі успішно зберігаються.
- Невалідні параметри відхиляються з помилкою валідації DTO (`BadRequestException`).

---

---

### [ ] TC-CONT-U-004: LibraryBlockService.update — режим 'update everywhere' оновлює версії у записах

| Поле         | Значення         |
| ------------ | ---------------- |
| **US**       | US-059           |
| **Issue**    | #86              |
| **TS**       | —                |
| **Метод**    | Unit (Jest)      |
| **Техніка**  | State Transition |
| **Priority** | P2               |

**Кроки:**

1. Створити запис із використанням готового блоку з бібліотеки.
2. Оновити цей блок у бібліотеці з вибором стратегії `UPDATE_EVERYWHERE`.
3. Перевірити версію та контент блоку всередині запису.

**Очікуваний результат:**

- Блок у записі автоматично синхронізується з новою версією бібліотечного блоку.

---

---

### [ ] TC-CONT-U-005: LibraryBlockService.update — режим 'save locally' розриває зв'язок з бібліотекою

| Поле         | Значення         |
| ------------ | ---------------- |
| **US**       | US-059           |
| **Issue**    | #86              |
| **TS**       | —                |
| **Метод**    | Unit (Jest)      |
| **Техніка**  | State Transition |
| **Priority** | P2               |

**Кроки:**

1. Створити запис із використанням готового блоку з бібліотеки.
2. Викликати оновлення з вибором стратегії `SAVE_LOCALLY` для цього запису.
3. Оновити блок у бібліотеці.
4. Перевірити стан блоку в записі.

**Очікуваний результат:**

- Зв'язок екземпляру блоку у записі з бібліотечним розривається (`libraryBlockId` стає `null`).
- Наступні оновлення бібліотеки не впливають на локальний блок у записі.

---

---

### [ ] TC-CONT-U-006: ContentAreaHistoryService.undoRedo — збереження історії дій та станів

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-061             |
| **Issue**    | #111               |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Здійснити три послідовні редагування контенту запису (стан 1, стан 2, стан 3).
2. Викликати `undo()` два рази. Перевірити поточний стан.
3. Викликати `redo()` один раз. Перевірити поточний стан.

**Очікуваний результат:**

- Після двох `undo()` активним стає стан 1.
- Після одного `redo()` активним стає стан 2.
- Вся історія успішно зберігається на сервері.

---

---

### [ ] TC-CONT-U-007: ContentAreaSnapshotService.create — логіка створення знімка при великих змінах (>200 символів)

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-062                  |
| **Issue**    | #112                    |
| **TS**       | —                       |
| **Метод**    | Unit (Jest)             |
| **Техніка**  | Boundary Value Analysis |
| **Priority** | P2                      |

**Кроки:**

1. Внести текстову зміну розміром у 199 символів. Перевірити створення знімка.
2. Внести текстову зміну розміром у 201 символ. Перевірити створення знімка.
3. Виконати операцію додавання нового компонента зображення.

**Очікуваний результат:**

- Зміна 199 символів не ініціює створення автоматичного знімку.
- Зміна 201 символ автоматично створює новий `Snapshot`.
- Додавання компонента негайно створює `Snapshot`.

---

---

### [ ] TC-CONT-U-008: ContentAreaSnapshotService.restore — відновлення стану та ротація ліміту в 50 знімків

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-062                  |
| **Issue**    | #112                    |
| **TS**       | —                       |
| **Метод**    | Unit (Jest)             |
| **Техніка**  | Boundary Value Analysis |
| **Priority** | P2                      |

**Кроки:**

1. Створити 50 знімків для одного запису.
2. Створити 51-й знімок. Перевірити загальну кількість та видалення найстарішого.
3. Викликати відновлення контентної області з 10-го знімка.

**Очікуваний результат:**

- При створенні 51-го знімка найстаріший (1-й) видаляється, загальна кількість залишається 50.
- Виклик відновлення успішно перезаписує поточний контент на вміст обраного знімка.

---

---

---

### [ ] TC-CONT-U-009: ContentAreaService — Відкат транзакції при виникненні помилки

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-053             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Налаштувати мок репозиторію або Prisma клієнта на успішне виконання першої частини транзакції (наприклад, створення запису).
2. Налаштувати мок репозиторію так, щоб наступний крок транзакції або супутня дія викликали виняток/помилку.
3. Викликати відповідний метод сервісу/usecase в транзакції.
4. Перевірити, що всі зміни, виконані в межах транзакції, відкочуються і помилка прокидається вище.

**Очікуваний результат:**

- Транзакція відміняється (rollback).
- Жодні проміжні зміни не зберігаються в базі даних.
- Метод прокидає відповідну помилку.

---

---

### [ ] TC-CONT-U-010: ContentAreaService — Обмеження доступу до ресурсу, який не належить користувачу

| Поле         | Значення        |
| ------------ | --------------- |
| **US**       | US-053          |
| **Issue**    | —               |
| **TS**       | —               |
| **Метод**    | Unit (Jest)     |
| **Техніка**  | Branch Coverage |
| **Priority** | P1              |

**Кроки:**

1. Налаштувати мок репозиторію для повернення ресурсу, де `ownerId` відрізняється від ID поточного користувача.
2. Викликати метод сервісу для доступу або модифікації цього ресурсу від імені поточного користувача.
3. Перевірити, що метод викидає `ForbiddenException`.

**Очікуваний результат:**

- Метод повертає `ForbiddenException` та не виконує операцію.
