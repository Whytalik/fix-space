# Unit Tests: Automations

### [x] TC-AUTO-U-001: AutomationScheduler — парсинг та розрахунок розкладу тригерів

| Поле         | Значення       |
| ------------ | -------------- |
| **US**       | US-036         |
| **Issue**    | #59            |
| **TS**       | —              |
| **Метод**    | Unit (Jest)    |
| **Техніка**  | Boundary Value |
| **Priority** | P2             |

**Кроки:**

1. Передати розклад `"Every Monday at 09:00"`.
2. Передати розклад `"Every day at 12:00"`.
3. Передати розклад `"First day of month at 00:00"`.

**Очікуваний результат:**

- Крок 1: повертається cron-вираз `"0 9 * * 1"`, наступний запуск розраховано правильно.
- Крок 2: повертається cron-вираз `"0 12 * * *"`.
- Крок 3: повертається cron-вираз `"0 0 1 * *"`.

---

---

### [x] TC-AUTO-U-002: AutomationExecutionEngine — послідовне виконання ланцюжка дій

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-036             |
| **Issue**    | #59                |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P2                 |

**Кроки:**

1. Створити ланцюжок з 3 дій: 1) встановити статус запису, 2) заповнити дату, 3) створити новий запис в іншій базі.
2. Викликати виконання двигуна автоматизацій.
3. Створити ланцюжок, де 2-га дія завершується з помилкою.

**Очікуваний результат:**

- Крок 2: усі 3 дії виконано послідовно одна за одною.
- Крок 3: 1-ша дія виконана, 2-га викликає помилку, 3-тя дія не запускається (ланцюжок переривається).

---

---

### [x] TC-AUTO-U-003: AutomationExecutionEngine — захист від нескінченної рекурсії

| Поле         | Значення        |
| ------------ | --------------- |
| **US**       | US-036          |
| **Issue**    | #59             |
| **TS**       | —               |
| **Метод**    | Unit (Jest)     |
| **Техніка**  | Branch Coverage |
| **Priority** | P1              |

**Кроки:**

1. Налаштувати автоматизацію: КОЛИ змінюється поле `Status` -> ТО встановити поле `Status = Closed`.
2. Змінити `Status` на `Open` і запустити автоматизацію.
3. Перевірити глибину рекурсивних викликів.

**Очікуваний результат:**

- Перший запуск автоматизації успішно змінює `Status` на `Closed`.
- Повторна зміна (спричинена самою автоматизацією) виявляється двигуном за допомогою контекстного прапорця `isAutomatedEdit` або лічильника стеків, запуск блокується. Глибина викликів = 1.

---

---

### [x] TC-AUTO-U-004: AutomationExecutionEngine — запобігання каскадним викликам між БД

| Поле         | Значення        |
| ------------ | --------------- |
| **US**       | US-036          |
| **Issue**    | #59             |
| **TS**       | —               |
| **Метод**    | Unit (Jest)     |
| **Техніка**  | Branch Coverage |
| **Priority** | P2              |

**Кроки:**

1. Налаштувати автоматизацію в базі A: КОЛИ створюється запис -> ТО створити запис у базі B.
2. Налаштувати автоматизацію в базі B: КОЛИ створюється запис -> ТО встановити `Status = Approved`.
3. Створити запис у базі A та запустити двигун.

**Очікуваний результат:**

- Запис у базі A створює запис у базі B.
- Автоматизація бази B на створення нового запису НЕ запускається (запобігання каскадності A -> B -> ...).

---

---

### [x] TC-AUTO-U-005: AutomationLogService — обмеження кількості та обробка помилок логування

| Поле         | Значення       |
| ------------ | -------------- |
| **US**       | US-038         |
| **Issue**    | #60            |
| **TS**       | —              |
| **Метод**    | Unit (Jest)    |
| **Техніка**  | Boundary Value |
| **Priority** | P2             |

**Кроки:**

1. Додати 50 логів запусків автоматизації.
2. Додати 51-й лог.
3. Додати запис про помилку виконання дії та перевірити статус.

**Очікуваний результат:**

- Крок 2: кількість логів залишається 50, найстаріший лог видалено.
- Крок 3: лог створюється зі статусом `ERROR`, містить стек помилки, майбутні запуски автоматизації не блокуються.

---

---

### [x] TC-AUTO-U-006: AutomationService — валідація ліміту на базу даних

| Поле         | Значення       |
| ------------ | -------------- |
| **US**       | US-090         |
| **Issue**    | #117           |
| **TS**       | —              |
| **Метод**    | Unit (Jest)    |
| **Техніка**  | Boundary Value |
| **Priority** | P3             |

**Кроки:**

1. Створити 10 автоматизацій для однієї бази даних.
2. Спробувати створити 11-ту автоматизацію.

**Очікуваний результат:**

- Крок 1: усі 10 автоматизацій успішно створені.
- Крок 2: викидається виняток `BadRequestException` (перевищено ліміт 10 автоматизацій на базу).

---

---

---

### [x] TC-AUTO-U-007: AutomationService — Відкат транзакції при виникненні помилки

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-036             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Налаштувати мок репозиторію або Prisma клієнта на успішне виконання першої частини транзакції (наприклад, створення запису).
2. Налаштувати мок репозиторію так, щоб наступний крок транзакції або супутня дія викликали виняток/помилку.
3. Викликати відповідний метод сервісу/usecase в транзакції.
4. Перевірити, що всі зміни, виконані в межах транзакції, відкочуються і помилка прокидається вище.

**Очікуваний результат:**

- Транзакція відміняється (rollback).
- Жодні проміжні зміни не зберігаються в базі даних.
- Метод прокидає відповідну помилку.

---

---

### [x] TC-AUTO-U-008: AutomationService — Обмеження доступу до ресурсу, який не належить користувачу

| Поле         | Значення        |
| ------------ | --------------- |
| **US**       | US-036          |
| **Issue**    | —               |
| **TS**       | —               |
| **Метод**    | Unit (Jest)     |
| **Техніка**  | Branch Coverage |
| **Priority** | P1              |

**Кроки:**

1. Налаштувати мок репозиторію для повернення ресурсу, де `ownerId` відрізняється від ID поточного користувача.
2. Викликати метод сервісу для доступу або модифікації цього ресурсу від імені поточного користувача.
3. Перевірити, що метод викидає `ForbiddenException`.

**Очікуваний результат:**

- Метод повертає `ForbiddenException` та не виконує операцію.

---

---

### [x] TC-AUTO-U-009: AutomationService — Обробка помилки дублювання унікальних полів

| Поле         | Значення       |
| ------------ | -------------- |
| **US**       | US-036         |
| **Issue**    | —              |
| **TS**       | —              |
| **Метод**    | Unit (Jest)    |
| **Техніка**  | Error Guessing |
| **Priority** | P1             |

**Кроки:**

1. Налаштувати мок репозиторію для імітації порушення унікального обмеження у БД (наприклад, унікального імені чи email).
2. Викликати відповідний метод створення або оновлення сервісу.
3. Перевірити, що метод перехоплює помилку репозиторію та викидає `ConflictException`.

**Очікуваний результат:**

- Метод повертає `ConflictException` із повідомленням про помилку дублювання.
