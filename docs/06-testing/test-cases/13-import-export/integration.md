# Integration Tests: Import export

### [x] TC-IMP-001: Імпорт CSV — валідний файл

| Поле         | Значення                 |
| ------------ | ------------------------ |
| **US**       | US-028                   |
| **Issue**    | #49                      |
| **TS**       | —                        |
| **Метод**    | Integration (Supertest)  |
| **Техніка**  | Equivalence Partitioning |
| **Priority** | P2                       |

**Кроки:**

1. `POST /import-export/preview` з CSV-файлом (UTF-8, < 25 МБ)
2. Перевірити статус-код

**Очікуваний результат:**

- Статус: `200 OK`
- Показано звіт валідації перед підтвердженням

---

### [x] TC-IMP-002: Імпорт CSV — підтримка UTF-8

| Поле         | Значення                 |
| ------------ | ------------------------ |
| **US**       | US-028                   |
| **Issue**    | #49                      |
| **TS**       | —                        |
| **Метод**    | Integration (Supertest)  |
| **Техніка**  | Equivalence Partitioning |
| **Priority** | P2                       |

**Кроки:**

1. CSV з UTF-8 кодуванням, кириличні символи
2. `POST /import-export/preview` — перевірити що символи коректні

**Очікуваний результат:**

- Кириличні символи відображаються коректно

---

### [x] TC-IMP-003: Імпорт CSV — RELATION виключено

| Поле         | Значення                 |
| ------------ | ------------------------ |
| **US**       | US-028                   |
| **Issue**    | #49                      |
| **TS**       | —                        |
| **Метод**    | Integration (Supertest)  |
| **Техніка**  | Equivalence Partitioning |
| **Priority** | P2                       |

**Кроки:**

1. CSV містить колонку для RELATION-поля
2. Перевірити що RELATION-колонка виключена з маппінгу

**Очікуваний результат:**

- RELATION-колонка не доступна для маппінгу

---

### [x] TC-IMP-005: Маппінг — незіставлені колонки ігноруються

| Поле         | Значення                 |
| ------------ | ------------------------ |
| **US**       | US-028                   |
| **Issue**    | #49                      |
| **TS**       | —                        |
| **Метод**    | Integration (Supertest)  |
| **Техніка**  | Equivalence Partitioning |
| **Priority** | P2                       |

**Кроки:**

1. Залишити колонку без маппінгу в DTO
2. `POST /import-export/import`
3. Перевірити що дані імпортовано без незіставленої колонки

**Очікуваний результат:**

- Імпорт успішний, незіставлена колонка ігнорована

---

### [x] TC-IMP-006: Експорт CSV — вибір властивостей

| Поле         | Значення                 |
| ------------ | ------------------------ |
| **US**       | US-029                   |
| **Issue**    | #50                      |
| **TS**       | —                        |
| **Метод**    | Integration (Supertest)  |
| **Техніка**  | Equivalence Partitioning |
| **Priority** | P2                       |

**Кроки:**

1. `GET /import-export/export` з параметром `propertyIds`
2. Перевірити що файл завантажується

**Очікуваний результат:**

- CSV файл містить тільки обрані властивості

---

### [x] TC-IMP-007: Експорт CSV — застосовуються фільтри view

| Поле         | Значення                 |
| ------------ | ------------------------ |
| **US**       | US-029                   |
| **Issue**    | #50                      |
| **TS**       | —                        |
| **Метод**    | Integration (Supertest)  |
| **Техніка**  | Equivalence Partitioning |
| **Priority** | P2                       |

**Кроки:**

1. Надіслати запит `GET /import-export/export` з параметром `viewId` (де налаштовано фільтр)
2. Перевірити що завантажені тільки відфільтровані записи

**Очікуваний результат:**

- CSV містить тільки записи що відповідають фільтру

---

### [x] TC-IMP-013: Запит до захищеного ендпоінту без токена автентифікації

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-028                  |
| **Issue**    | —                       |
| **TS**       | —                       |
| **Метод**    | Integration (Supertest) |
| **Техніка**  | Security Testing        |
| **Priority** | P1                      |

**Кроки:**

1. Надіслати запит до будь-якого захищеного ендпоінту модуля Import & Export (наприклад, `POST /import-export/preview`) без заголовка `Authorization`.
2. Перевірити статус-код відповіді та повідомлення про помилку.

**Очікуваний результат:**

- Статус: `401 Unauthorized`
- Body містить повідомлення про відсутність або невалідність токена.

---

### [x] TC-IMP-014: Спроба доступу до ресурсу іншого користувача

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-028                  |
| **Issue**    | —                       |
| **TS**       | —                       |
| **Метод**    | Integration (Supertest) |
| **Техніка**  | Security Testing        |
| **Priority** | P1                      |

**Кроки:**

1. Авторизуватися під Користувачем А.
2. Спробувати надіслати запит `POST /import-export/preview` для бази даних, що належить Користувачу Б.
3. Перевірити статус-код відповіді.

**Очікуваний результат:**

- Статус: `404 Not Found` (Для безпеки система повертає 404 замість 403, щоб приховати існування бази даних іншого користувача).

---

### [x] TC-IMP-015: Спроба доступу до неіснуючого ресурсу

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-028                  |
| **Issue**    | —                       |
| **TS**       | —                       |
| **Метод**    | Integration (Supertest) |
| **Техніка**  | Error Guessing          |
| **Priority** | P2                      |

**Кроки:**

1. Авторизуватися в системі.
2. Спробувати надіслати запит `POST /import-export/preview` з неіснуючим UUID для `databaseId`.
3. Перевірити статус-код відповіді.

**Очікуваний результат:**

- Статус: `404 Not Found`
- Body містить повідомлення про те, що база даних не знайдена.
