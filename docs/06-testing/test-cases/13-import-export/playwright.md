# Playwright UI E2E Tests: Import export

### [x] TC-IMP-004: Маппінг колонок — ручне зіставлення

| Поле         | Значення            |
| ------------ | ------------------- |
| **US**       | US-028              |
| **Issue**    | #49                 |
| **TS**       | —                   |
| **Метод**    | Manual (Browser)    |
| **Техніка**  | Exploratory Testing |
| **Priority** | P2                  |

**Кроки:**

1. Завантажити CSV з колонками що не збігаються з назвами властивостей
2. Відкрити таблицю маппінгу
3. Вручну зіставити колонки

**Очікуваний результат:**

- Таблиця маппінгу з попереднім переглядом даних

---

### [x] TC-IMP-008: Експорт CSV — файл завантажується автоматично

| Поле         | Значення            |
| ------------ | ------------------- |
| **US**       | US-029              |
| **Issue**    | #50                 |
| **TS**       | —                   |
| **Метод**    | Manual (Browser)    |
| **Техніка**  | Exploratory Testing |
| **Priority** | P2                  |

**Кроки:**

1. Натиснути "Export CSV"
2. Перевірити що файл завантажується в браузері

**Очікуваний результат:**

- Файл завантажується автоматично

---

### [x] TC-IMP-009: Звіт валідації — відображення помилок

| Поле         | Значення         |
| ------------ | ---------------- |
| **US**       | US-084           |
| **Issue**    | #104             |
| **TS**       | —                |
| **Метод**    | Manual (Browser) |
| **Техніка**  | Error Guessing   |
| **Priority** | P3               |

**Кроки:**

1. Завантажити CSV з помилками (невалідні дані)
2. Перевірити звіт валідації

**Очікуваний результат:**

- Відображаються рядки з помилками та причини

---

### [x] TC-IMP-010: Звіт валідації — скасування або підтвердження

| Поле         | Значення         |
| ------------ | ---------------- |
| **US**       | US-084           |
| **Issue**    | #104             |
| **TS**       | —                |
| **Метод**    | Manual (Browser) |
| **Техніка**  | State Transition |
| **Priority** | P3               |

**Кроки:**

1. Отримати звіт з помилками
2. Натиснути "Cancel" — перевірити що імпорт скасовано
3. Повторити, натиснути "Import anyway" — перевірити що імпорт виконано

**Очікуваний результат:**

- Cancel: імпорт скасовано
- Import anyway: дані імпортовано (помилкові рядки пропущено)

---

### [x] TC-IMP-011: Експорт бази даних у форматі CSV через меню опцій (Playwright UI E2E)

| Поле         | Значення            |
| ------------ | ------------------- |
| **US**       | US-029              |
| **Issue**    | #50                 |
| **TS**       | —                   |
| **Метод**    | E2E UI (Playwright) |
| **Техніка**  | Exploratory Testing |
| **Priority** | P2                  |

**Кроки:**

1. Увійти в систему та відкрити існуючу базу даних (наприклад, Trading Journal).
2. Натиснути на кнопку меню опцій бази даних (іконка з трьома крапками або шестірня у правому верхньому кутку).
3. Обрати пункт меню "Експортувати в CSV" (Export to CSV).
4. У вікні налаштування експорту вибрати всі колонки та підтвердити дію.
5. Зачекати завершення генерації та переконатися, що браузер розпочав і успішно завершив завантаження файлу з розширенням `.csv`.

**Очікуваний результат:**

- Браузер успішно завантажує файл CSV, що містить валідний звіт по записах бази даних.

---

### [x] TC-IMP-012: Імпорт записів з CSV файлу через UI (Playwright)

| Поле         | Значення                              |
| ------------ | ------------------------------------- |
| **US**       | US-084                                |
| **Issue**    | —                                     |
| **TS**       | —                                     |
| **Метод**    | E2E UI (Playwright)                   |
| **Техніка**  | Equivalence Partitioning (valid path) |
| **Priority** | P3                                    |

**Кроки:**

1. Відкрити меню бази даних та знайти опцію Import
2. Вибрати файл CSV із заздалегідь підготовленими тестовими записами
3. Підтвердити маппінг колонок та розпочати імпорт
4. Дочекатися завершення та перевірити кількість доданих записів у таблиці

**Очікуваний результат:**

- Записи з CSV файлу успішно імпортуються до бази даних
- Кількість записів у таблиці збільшується відповідно до вмісту файлу
