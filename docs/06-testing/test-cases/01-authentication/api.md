# Manual API Tests: Authentication

### [ ] TC-AUTH-006: Верифікація з простроченим токеном

| Поле         | Значення                          |
| ------------ | --------------------------------- |
| **US**       | US-002                            |
| **Issue**    | #2                                |
| **TS**       | TS-AUTH-02                        |
| **Метод**    | Manual (Postman)                  |
| **Техніка**  | Boundary Value Analysis (expired) |
| **Priority** | P1                                |

**Кроки:**

1. Зареєструвати користувача
2. В БД змінити `expiresAt` токена на минулий час (`Date.now() - 1000`)
3. `POST /auth/verify` з тілом: `{ "token": "<raw-token>" }`
4. Перевірити статус-код

**Очікуваний результат:**

- Статус: `400 Bad Request`
- Body: `{ "message": "Invalid or expired verification token" }`

---

### [ ] TC-AUTH-025: Видалення акаунту — валідний запит

| Поле         | Значення                         |
| ------------ | -------------------------------- |
| **US**       | US-071                           |
| **Issue**    | #99                              |
| **TS**       | —                                |
| **Метод**    | Manual (Postman)                 |
| **Техніка**  | Equivalence Partitioning (valid) |
| **Priority** | P2                               |

**Кроки:**

1. Увійти в систему
2. `DELETE /users/me` з тілом: `{ "password": "Test1234!" }`
3. Перевірити статус-код
4. Перевірити БД (користувач видалений)

**Очікуваний результат:**

- Статус: `200 OK`
- Користувач видалений з БД (каскадне видалення всіх даних)
- Всі сесії закриті

---

### [ ] TC-AUTH-026: Видалення акаунту — неправильний пароль

| Поле         | Значення         |
| ------------ | ---------------- |
| **US**       | US-071           |
| **Issue**    | #99              |
| **TS**       | —                |
| **Метод**    | Manual (Postman) |
| **Техніка**  | Error Guessing   |
| **Priority** | P2               |

**Кроки:**

1. `DELETE /users/me` з тілом: `{ "password": "WrongPassword!" }`
2. Перевірити статус-код

**Очікуваний результат:**

- Статус: `401 Unauthorized`
- Акаунт НЕ видалений

---

### [ ] TC-AUTH-027: Видалення акаунту — Google-only без пароля

| Поле         | Значення         |
| ------------ | ---------------- |
| **US**       | US-071           |
| **Issue**    | #99              |
| **TS**       | —                |
| **Метод**    | Manual (Postman) |
| **Техніка**  | Error Guessing   |
| **Priority** | P2               |

**Кроки:**

1. Увійти через Google (акаунт без локального пароля)
2. `DELETE /users/me` з порожнім тілом `{}`
3. Перевірити статус-код

**Очікуваний результат:**

- Статус: `200 OK`
- Акаунт видалений (перевірка пароля пропускається для Google-only акаунтів)
