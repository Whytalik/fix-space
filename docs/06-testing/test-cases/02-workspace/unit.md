# Unit Tests: Workspace

### [x] TC-WS-U-001: SpaceService.create — Створення простору з налаштуваннями за замовчуванням

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-021             |
| **Issue**    | #35                |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати `SpaceService.create(ownerId, { name: "New Space", isDefault: true })`
2. Перевірити, що `SpaceRepository.create` викликається з правильними параметрами (включаючи `sectionsInclude`)
3. Перевірити, що повертається `SpaceResponseDto` з очікуваними значеннями

**Очікуваний результат:**

- `SpaceRepository.create` викликано з `{ name: "New Space", isDefault: true, ownerId }` та `sectionsInclude`
- Метод повертає об'єкт `SpaceResponseDto` з новоствореним простором

---

---

### [x] TC-WS-U-002: SpaceService.findOne — Помилка у разі відсутності простору

| Поле         | Значення        |
| ------------ | --------------- |
| **US**       | US-021          |
| **Issue**    | #36             |
| **TS**       | —               |
| **Метод**    | Unit (Jest)     |
| **Техніка**  | Branch Coverage |
| **Priority** | P1              |

**Кроки:**

1. Налаштувати `SpaceRepository.findOne` так, щоб він повертав `null` для певного `id`
2. Викликати `SpaceService.findOne(id)`
3. Перевірити, що метод викидає `NotFoundException`

**Очікуваний результат:**

- Виклик `SpaceService.findOne(id)` призводить до викидання `NotFoundException` із відповідним повідомленням про помилку

---

---

### [x] TC-WS-U-003: SpaceService.update — Оновлення та скидання статусу основних просторів

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-021             |
| **Issue**    | #36                |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Налаштувати репозиторій для повернення поточного простору та його власника
2. Викликати `SpaceService.update(id, { name: "Updated Name", isDefault: true })`
3. Перевірити, що `SpaceRepository.updateMany` викликається для скидання `isDefault: false` у інших просторів користувача
4. Перевірити, що `SpaceRepository.update` викликається з оновленими полями в межах транзакції

**Очікуваний результат:**

- Метод повертає оновлений `SpaceResponseDto`
- Інші простори користувача оновлені на `isDefault: false`

---

---

### [x] TC-WS-U-004: SpaceService.remove — Заборона видалення основного простору

| Поле         | Значення        |
| ------------ | --------------- |
| **US**       | US-021          |
| **Issue**    | #37             |
| **TS**       | —               |
| **Метод**    | Unit (Jest)     |
| **Техніка**  | Branch Coverage |
| **Priority** | P1              |

**Кроки:**

1. Налаштувати репозиторій на повернення простору з `isDefault: true` для `id`
2. Викликати `SpaceService.remove(id)`
3. Перевірити викидання `BadRequestException`
4. Налаштувати репозиторій на повернення простору з `isDefault: false`, виконати `remove(id)`, перевірити успішне видалення через `SpaceRepository.delete`

**Очікуваний результат:**

- При `isDefault: true` викидається `BadRequestException`
- При `isDefault: false` простір успішно видаляється і повертається видалений `SpaceResponseDto`

---

---

### [x] TC-WS-U-005: DuplicateSpaceUseCase — Копіювання структури без втрати зв'язків

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-064             |
| **Issue**    | #92                |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Налаштувати репозиторій для повернення вихідного простору з секціями, базами, властивостями, записами та значеннями
2. Викликати `DuplicateSpaceUseCase.execute(id, ownerId, { newName: "Duplicated Space" })`
3. Перевірити, що в транзакції створюється новий простір, копіюються всі секції, бази даних, властивості та записи з урахуванням мапінгу ідентифікаторів
4. Перевірити, що унікальні назви генеруються коректно

**Очікуваний результат:**

- Новий простір успішно створено
- Всі вкладені об'єкти (секції, бази, властивості, записи та значення) скопійовані з коректними ідентифікаторами зв'язків

---

---

### [x] TC-WS-U-006: InitializeUserSpaceUseCase — Ініціалізація стартового пакету при реєстрації

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-004             |
| **Issue**    | #6                 |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Налаштувати `InitializationConfigService` на повернення стартового пакету конфігурації (секції, бази, шаблони, властивості, зв'язки)
2. Викликати `InitializeUserSpaceUseCase.seedContent(spaceId, userId)`
3. Перевірити послідовність створення секцій, баз даних та властивостей
4. Перевірити, що властивості типу `RELATION` створюються після розв'язання відповідних баз даних

**Очікуваний результат:**

- Секції та бази даних створені відповідно до конфігурації
- Зв'язки типу `RELATION` мають коректні `relatedEntityId` після ініціалізації всіх баз даних

---

---

---

### [x] TC-WS-U-007: SpaceService — Відкат транзакції при виникненні помилки

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-021             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Налаштувати мок репозиторію або Prisma клієнта на успішне виконання першої частини транзакції (наприклад, створення запису).
2. Налаштувати мок репозиторію так, щоб наступний крок транзакції або супутня дія викликали виняток/помилку.
3. Викликати відповідний метод сервісу/usecase в транзакції.
4. Перевірити, що всі зміни, виконані в межах транзакції, відкочуються і помилка прокидається вище.

**Очікуваний результат:**

- Транзакція відміняється (rollback).
- Жодні проміжні зміни не зберігаються в базі даних.
- Метод прокидає відповідну помилку.

---

---

### [x] TC-WS-U-008: SpaceService — Обмеження доступу до ресурсу, який не належить користувачу

| Поле         | Значення        |
| ------------ | --------------- |
| **US**       | US-021          |
| **Issue**    | —               |
| **TS**       | —               |
| **Метод**    | Unit (Jest)     |
| **Техніка**  | Branch Coverage |
| **Priority** | P1              |

**Кроки:**

1. Налаштувати мок репозиторію для повернення ресурсу, де `ownerId` відрізняється від ID поточного користувача.
2. Викликати метод сервісу для доступу або модифікації цього ресурсу від імені поточного користувача.
3. Перевірити, що метод викидає `ForbiddenException`.

**Очікуваний результат:**

- Метод повертає `ForbiddenException` та не виконує операцію.

---

---

### [x] TC-WS-U-009: SpaceService — Обробка помилки дублювання унікальних полів

| Поле         | Значення       |
| ------------ | -------------- |
| **US**       | US-021         |
| **Issue**    | —              |
| **TS**       | —              |
| **Метод**    | Unit (Jest)    |
| **Техніка**  | Error Guessing |
| **Priority** | P1             |

**Кроки:**

1. Налаштувати мок репозиторію для імітації порушення унікального обмеження у БД (наприклад, унікального імені чи email).
2. Викликати відповідний метод створення або оновлення сервісу.
3. Перевірити, що метод перехоплює помилку репозиторію та викидає `ConflictException`.

**Очікуваний результат:**

- Метод повертає `ConflictException` із повідомленням про помилку дублювання.

---

### [x] TC-WS-U-010: SpaceService.create — BadRequestException при перевищенні ліміту (5 просторів)

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-021                  |
| **Issue**    | —                       |
| **TS**       | —                       |
| **Метод**    | Unit (Jest)             |
| **Техніка**  | Boundary Value Analysis |
| **Priority** | P1                      |

**Кроки:**

1. Мок `spaceRepo.count` повертає `5`.
2. Викликати `SpaceService.create(ownerId, { name: "New Space" })`.

**Очікуваний результат:**

- Кидається `BadRequestException` з повідомленням про перевищення ліміту.
- `spaceRepo.create` НЕ викликається.

---

### [x] TC-WS-U-011: SpaceService.findAll — повертає список просторів

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-021             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Мок `spaceRepo.findAll` повертає масив із двох об'єктів просторів.
2. Викликати `SpaceService.findAll("user-1")`.

**Очікуваний результат:**

- Повертається масив із двох `SpaceResponseDto`.
- `spaceRepo.findAll("user-1", sectionsInclude)` викликано.

---

### [x] TC-WS-U-012: GetDashboardUseCase.execute — повертає активні сесії та записи

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-021             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P2                 |

**Кроки:**

1. Мок `spaceRepo.findOne` повертає простір з `id`.
2. Мок `recordRepo` повертає записи щоденних рутин.
3. Викликати `GetDashboardUseCase.execute("space-1")`.

**Очікуваний результат:**

- Повертається `DashboardResponseDto` з полями `activeSessions`, `nextSession`, `todayRoutines`.
- NotFoundException кидається якщо простір не знайдено.
