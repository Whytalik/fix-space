# Unit Tests: Records

### [ ] TC-REC-U-001: RecordService.create — створення з шаблоном та заповнення значень

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | #22                |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати `RecordService.create()` передавши `databaseId` та `templateId` (кастомний шаблон із заздалегідь визначеними значеннями властивостей).
2. Перевірити копіювання значень властивостей із шаблону в створюваний запис.
3. Перевірити копіювання структури контентної області.

**Очікуваний результат:**

- Запис створюється успішно.
- Значення властивостей нового запису точно копіюють налаштування з шаблону.
- Контентна область дублюється із структури шаблону.

---

---

### [ ] TC-REC-U-002: RecordService.create — токенізація назви з шаблону

| Поле         | Значення                 |
| ------------ | ------------------------ |
| **US**       | US-005                   |
| **Issue**    | #22                      |
| **TS**       | —                        |
| **Метод**    | Unit (Jest)              |
| **Техніка**  | Equivalence Partitioning |
| **Priority** | P1                       |

**Кроки:**

1. Створити шаблон із Name Pattern: `"Daily Check {{today}} #{{count}}"`
2. Викликати `RecordService.create()` з цим шаблоном.
3. Перевірити сформовану назву запису.

**Очікуваний результат:**

- Токени парсяться та замінюються на динамічні значення (наприклад, `"Daily Check 04.06.2026 #5"`).

---

---

### [ ] TC-REC-U-003: RecordService.duplicate — копіювання властивостей без контентної області

| Поле         | Значення         |
| ------------ | ---------------- |
| **US**       | US-049           |
| **Issue**    | #13              |
| **TS**       | —                |
| **Метод**    | Unit (Jest)      |
| **Техніка**  | State Transition |
| **Priority** | P2               |

**Кроки:**

1. Викликати `RecordService.duplicate()` для існуючого запису з заповненими властивостями та текстом у контентній області.
2. Перевірити властивості дубльованого запису.
3. Перевірити контентну область дубльованого запису.

**Очікуваний результат:**

- Створюється копія запису з назвою `"Copy of [Назва]"` (або аналогічною).
- Значення всіх властивостей скопійовані.
- Контентна область нового запису залишається порожньою (згідно з вимогами).

---

---

### [ ] TC-REC-U-006: RecordService.bulkEdit — масове редагування сумісних типів властивостей

| Поле         | Значення                 |
| ------------ | ------------------------ |
| **US**       | US-051                   |
| **Issue**    | #22                      |
| **TS**       | —                        |
| **Метод**    | Unit (Jest)              |
| **Техніка**  | Equivalence Partitioning |
| **Priority** | P2                       |

**Кроки:**

1. Викликати `RecordService.bulkEdit()` для списку з 3-х записів.
2. Спробувати оновити властивість типу `SELECT` на нове значення.
3. Перевірити оновлені записи.

**Очікуваний результат:**

- Значення властивості оновлено для всіх обраних записів.
- Статус відповіді `200 OK`.

---

---

### [ ] TC-REC-U-007: RecordService.bulkEdit — заборона редагування FORMULA та RELATION

| Поле         | Значення                             |
| ------------ | ------------------------------------ |
| **US**       | US-051                               |
| **Issue**    | #22                                  |
| **TS**       | —                                    |
| **Метод**    | Unit (Jest)                          |
| **Техніка**  | Equivalence Partitioning (protected) |
| **Priority** | P2                                   |

**Кроки:**

1. Викликати `RecordService.bulkEdit()` для списку записів з метою оновлення властивості типу `FORMULA`.
2. Викликати `RecordService.bulkEdit()` для властивості типу `RELATION`.

**Очікуваний результат:**

- Обидва виклики повертають помилку `BadRequestException` або `ForbiddenException`, оскільки формули та зв'язки не підлягають масовому редагуванню.

---

---

### [ ] TC-REC-U-008: RecordService.purgeExpired — видалення записів старших за 30 днів

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-030                  |
| **Issue**    | #22                     |
| **TS**       | —                       |
| **Метод**    | Unit (Jest)             |
| **Техніка**  | Boundary Value Analysis |
| **Priority** | P2                      |

**Кроки:**

1. Створити запис із `deletedAt` рівним 31 день тому.
2. Створити запис із `deletedAt` рівним 29 днів тому.
3. Викликати крон-функцію `RecordService.purgeExpired()`.
4. Перевірити наявність записів у базі даних.

**Очікуваний результат:**

- Запис старше 30 днів видаляється перманентно з бази даних.
- Запис, видалений менше 30 днів тому, залишається з позначкою `deletedAt`.

---

---

---

### [ ] TC-REC-U-009: RecordService — Відкат транзакції при виникненні помилки

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Налаштувати мок репозиторію або Prisma клієнта на успішне виконання першої частини транзакції (наприклад, створення запису).
2. Налаштувати мок репозиторію так, щоб наступний крок транзакції або супутня дія викликали виняток/помилку.
3. Викликати відповідний метод сервісу/usecase в транзакції.
4. Перевірити, що всі зміни, виконані в межах транзакції, відкочуються і помилка прокидається вище.

**Очікуваний результат:**

- Транзакція відміняється (rollback).
- Жодні проміжні зміни не зберігаються в базі даних.
- Метод прокидає відповідну помилку.

---

---

### [ ] TC-REC-U-010: RecordService — Обмеження доступу до ресурсу, який не належить користувачу

| Поле         | Значення        |
| ------------ | --------------- |
| **US**       | US-005          |
| **Issue**    | —               |
| **TS**       | —               |
| **Метод**    | Unit (Jest)     |
| **Техніка**  | Branch Coverage |
| **Priority** | P1              |

**Кроки:**

1. Налаштувати мок репозиторію для повернення ресурсу, де `ownerId` відрізняється від ID поточного користувача.
2. Викликати метод сервісу для доступу або модифікації цього ресурсу від імені поточного користувача.
3. Перевірити, що метод викидає `ForbiddenException`.

**Очікуваний результат:**

- Метод повертає `ForbiddenException` та не виконує операцію.
