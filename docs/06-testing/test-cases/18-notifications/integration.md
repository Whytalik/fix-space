# Integration Tests: Notifications

### [x] TC-NOTIF-001: In-app сповіщення — Integration error

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-072                  |
| **Issue**    | #102                    |
| **TS**       | —                       |
| **Метод**    | Integration (Supertest) |
| **Техніка**  | State Transition        |
| **Priority** | P2                      |

**Кроки:**

1. Зімітувати помилку інтеграції (створити запис сповіщення з типом `INTEGRATION`)
2. `GET /notifications` — перевірити відображення сповіщення

**Очікуваний результат:**

- Сповіщення типу "Integration" з текстом помилки присутнє

---

### [x] TC-NOTIF-002: In-app сповіщення — Automation result

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-072                  |
| **Issue**    | #102                    |
| **TS**       | —                       |
| **Метод**    | Integration (Supertest) |
| **Техніка**  | State Transition        |
| **Priority** | P2                      |

**Кроки:**

1. Створити сповіщення про успішний/помилковий результат автоматизації
2. `GET /notifications` — перевірити наявність сповіщення

**Очікуваний результат:**

- Сповіщення типу "Automation" з текстом результату присутнє

---

### [x] TC-NOTIF-012: Запит до захищеного ендпоінту без токена автентифікації

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-072                  |
| **Issue**    | —                       |
| **TS**       | —                       |
| **Метод**    | Integration (Supertest) |
| **Техніка**  | Security Testing        |
| **Priority** | P1                      |

**Кроки:**

1. Надіслати запит до ендпоінту `GET /notifications` без заголовка `Authorization`.
2. Перевірити статус-код відповіді та повідомлення про помилку.

**Очікуваний результат:**

- Статус: `401 Unauthorized`
- Body містить повідомлення про відсутність або невалідність токена.

---

### [x] TC-NOTIF-013: Спроба доступу до ресурсу іншого користувача

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-072                  |
| **Issue**    | —                       |
| **TS**       | —                       |
| **Метод**    | Integration (Supertest) |
| **Техніка**  | Security Testing        |
| **Priority** | P1                      |

**Кроки:**

1. Авторизуватися під Користувачем А.
2. Спробувати надіслати запит `PATCH /notifications/:id/read` для сповіщення, що належить Користувачу Б.
3. Перевірити статус-код відповіді.

**Очікуваний результат:**

- Статус: `404 Not Found` (Для безпеки система повертає 404 замість 403, щоб приховати існування сповіщення іншого користувача).

---

### [x] TC-NOTIF-014: Спроба доступу до неіснуючого ресурсу

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-072                  |
| **Issue**    | —                       |
| **TS**       | —                       |
| **Метод**    | Integration (Supertest) |
| **Техніка**  | Error Guessing          |
| **Priority** | P2                      |

**Кроки:**

1. Авторизуватися в системі.
2. Спробувати надіслати запит `PATCH /notifications/00000000-0000-0000-0000-000000000000/read` з неіснуючим UUID для `id`.
3. Перевірити статус-код відповіді.

**Очікуваний результат:**

- Статус: `404 Not Found`
- Body містить повідомлення про те, що ресурс не знайдено.
