# Integration Tests: Views

### [x] TC-VIEW-004: Прихована колонка — дані не впливають

| Поле         | Значення                 |
| ------------ | ------------------------ |
| **US**       | US-007                   |
| **Issue**    | #17                      |
| **TS**       | TS-REC-01                |
| **Метод**    | Integration (Supertest)  |
| **Техніка**  | Equivalence Partitioning |
| **Priority** | P1                       |

**Кроки:**

1. Створити view з прихованою колонкою
2. `GET /records` — перевірити що значення властивості доступне

**Очікуваний результат:**

- Значення властивості доступне через API (приховано тільки в UI)

---

### [x] TC-VIEW-023: Кілька view — створення

| Поле         | Значення                 |
| ------------ | ------------------------ |
| **US**       | US-043                   |
| **Issue**    | #67                      |
| **TS**       | —                        |
| **Метод**    | Integration (Supertest)  |
| **Техніка**  | Equivalence Partitioning |
| **Priority** | P2                       |

**Кроки:**

1. `POST /databases/:databaseId/views` з тілом: `{ "name": "My View", "type": "table" }`
2. Перевірити що view створено

**Очікуваний результат:**

- Статус: `201 Created`
- View має власні фільтри, сортування, вибір типу

---

### [x] TC-VIEW-025: Блокування view — зміна фільтрів заборонена

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-044                  |
| **Issue**    | #68                     |
| **TS**       | —                       |
| **Метод**    | Integration (Supertest) |
| **Техніка**  | State Transition        |
| **Priority** | P2                      |

**Кроки:**

1. Заблокувати view: `PATCH /views/:id` з `{ "isLocked": true }`
2. Спробувати змінити фільтри
3. Перевірити статус-код

**Очікуваний результат:**

- Статус: `403 Forbidden`
- Body: повідомлення що view заблоковано

---

### [x] TC-VIEW-027: AND/OR логіка фільтрів — AND

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-045                  |
| **Issue**    | #69                     |
| **TS**       | —                       |
| **Метод**    | Integration (Supertest) |
| **Техніка**  | Decision Tables         |
| **Priority** | P2                      |

**Кроки:**

1. Застосувати 2 фільтри з AND логікою: `Name contains Trade` AND `Asset equals EURUSD`
2. Перевірити результати

**Очікуваний результат:**

- Тільки записи що відповідають обом умовам повернуті API

---

### [x] TC-VIEW-028: AND/OR логіка фільтрів — OR

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-045                  |
| **Issue**    | #69                     |
| **TS**       | —                       |
| **Метод**    | Integration (Supertest) |
| **Техніка**  | Decision Tables         |
| **Priority** | P2                      |

**Кроки:**

1. Застосувати 2 фільтри з OR логікою: `Asset equals GBPUSD` OR `Name equals Trade 3`
2. Перевірити результати

**Очікуваний результат:**

- Записи що відповідають хоча б одній умові повернуті API

---

### [x] TC-VIEW-031: Запит до захищеного ендпоінту без токена автентифікації

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-007                  |
| **Issue**    | —                       |
| **TS**       | —                       |
| **Метод**    | Integration (Supertest) |
| **Техніка**  | Security Testing        |
| **Priority** | P1                      |

**Кроки:**

1. Надіслати запит до будь-якого захищеного ендпоінту модуля Views (наприклад, `GET /databases/:databaseId/views`) без заголовка `Authorization`.
2. Перевірити статус-код відповіді та повідомлення про помилку.

**Очікуваний результат:**

- Статус: `401 Unauthorized`
- Body містить повідомлення про відсутність або невалідність токена.

---

### [x] TC-VIEW-032: Спроба доступу до ресурсу іншого користувача

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-007                  |
| **Issue**    | —                       |
| **TS**       | —                       |
| **Метод**    | Integration (Supertest) |
| **Техніка**  | Security Testing        |
| **Priority** | P1                      |

**Кроки:**

1. Авторизуватися під Користувачем А.
2. Спробувати виконати операцію читання/редагування/видалення ресурсу, що належить Користувачу Б (надіслати запит до ендпоінту з ID ресурсу Користувача Б).
3. Перевірити статус-код відповіді.

**Очікуваний результат:**

- Статус: `403 Forbidden` або `404 Not Found` (якщо база даних чужа)

---

### [x] TC-VIEW-033: Спроба доступу до неіснуючого ресурсу

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-007                  |
| **Issue**    | —                       |
| **TS**       | —                       |
| **Метод**    | Integration (Supertest) |
| **Техніка**  | Error Guessing          |
| **Priority** | P2                      |

**Кроки:**

1. Авторизуватися в системі.
2. Спробувати надіслати запит для отримання, оновлення або видалення ресурсу модуля Views з неіснуючим ID (наприклад, `00000000-0000-0000-0000-000000000000` або `999999`).
3. Перевірити статус-код відповіді.

**Очікуваний результат:**

- Статус: `404 Not Found`
- Body містить повідомлення про те, що ресурс не знайдено.
