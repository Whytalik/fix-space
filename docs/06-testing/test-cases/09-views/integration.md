# Integration Tests: Views

### [ ] TC-VIEW-004: Прихована колонка — дані не впливають

| Поле         | Значення                 |
| ------------ | ------------------------ |
| **US**       | US-007                   |
| **Issue**    | #17                      |
| **TS**       | TS-REC-01                |
| **Метод**    | Integration (Supertest)  |
| **Техніка**  | Equivalence Partitioning |
| **Priority** | P1                       |

**Кроки:**

1. Приховати колонку
2. `GET /records/:id` — перевірити що значення властивості доступне

**Очікуваний результат:**

- Значення властивості доступне через API (приховано тільки в UI)

---

### [ ] TC-VIEW-021: Пресет фільтрів — збереження

| Поле         | Значення                 |
| ------------ | ------------------------ |
| **US**       | US-042                   |
| **Issue**    | #66                      |
| **TS**       | —                        |
| **Метод**    | Integration (Supertest)  |
| **Техніка**  | Equivalence Partitioning |
| **Priority** | P2                       |

**Кроки:**

1. Застосувати фільтри
2. `POST /views/:id/filter-presets` з тілом: `{ "name": "Winning Trades" }`
3. Перевірити що пресет збережено

**Очікуваний результат:**

- Статус: `201 Created`
- Пресет збережений з поточними фільтрами

---

### [ ] TC-VIEW-022: Пресет фільтрів — застосування одним кліком

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-042                  |
| **Issue**    | #66                     |
| **TS**       | —                       |
| **Метод**    | Integration (Supertest) |
| **Техніка**  | State Transition        |
| **Priority** | P2                      |

**Кроки:**

1. Зберегти пресет (TC-VIEW-021)
2. Скинути фільтри
3. Застосувати пресет
4. Перевірити що фільтри відновлені

**Очікуваний результат:**

- Фільтри пресету застосовані

---

### [ ] TC-VIEW-023: Кілька view — створення

| Поле         | Значення                 |
| ------------ | ------------------------ |
| **US**       | US-043                   |
| **Issue**    | #67                      |
| **TS**       | —                        |
| **Метод**    | Integration (Supertest)  |
| **Техніка**  | Equivalence Partitioning |
| **Priority** | P2                       |

**Кроки:**

1. `POST /views` з тілом: `{ "databaseId": "<db-id>", "name": "My View", "type": "table" }`
2. Перевірити що view створено

**Очікуваний результат:**

- Статус: `201 Created`
- View має власні фільтри, сортування, видимість колонок

---

### [ ] TC-VIEW-025: Блокування view — зміна фільтрів заборонена

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-044                  |
| **Issue**    | #68                     |
| **TS**       | —                       |
| **Метод**    | Integration (Supertest) |
| **Техніка**  | State Transition        |
| **Priority** | P2                      |

**Кроки:**

1. Заблокувати view: `PATCH /views/:id` з `{ "isLocked": true }`
2. Спробувати змінити фільтри
3. Перевірити статус-код

**Очікуваний результат:**

- Статус: `403 Forbidden`
- Body: повідомлення що view заблоковано

---

### [ ] TC-VIEW-027: AND/OR логіка фільтрів — AND

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-045                  |
| **Issue**    | #69                     |
| **TS**       | —                       |
| **Метод**    | Integration (Supertest) |
| **Техніка**  | Decision Tables         |
| **Priority** | P2                      |

**Кроки:**

1. Застосувати 2 фільтри з AND логікою: `Status = Closed` AND `Result = Win`
2. Перевірити результати

**Очікуваний результат:**

- Тільки записи що відповідають обом умовам

---

### [ ] TC-VIEW-028: AND/OR логіка фільтрів — OR

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-045                  |
| **Issue**    | #69                     |
| **TS**       | —                       |
| **Метод**    | Integration (Supertest) |
| **Техніка**  | Decision Tables         |
| **Priority** | P2                      |

**Кроки:**

1. Застосувати 2 фільтри з OR логікою: `Result = Win` OR `Result = Break-even`
2. Перевірити результати

**Очікуваний результат:**

- Записи що відповідають хоча б одній умові

---

### [ ] TC-VIEW-031: Запит до захищеного ендпоінту без токена автентифікації

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-007                  |
| **Issue**    | —                       |
| **TS**       | —                       |
| **Метод**    | Integration (Supertest) |
| **Техніка**  | Security Testing        |
| **Priority** | P1                      |

**Кроки:**

1. Надіслати запит до будь-якого захищеного ендпоінту модуля Views (наприклад, `GET /views`) без заголовка `Authorization`.
2. Перевірити статус-код відповіді та повідомлення про помилку.

**Очікуваний результат:**

- Статус: `401 Unauthorized`
- Body містить повідомлення про відсутність або невалідність токена.

---

### [ ] TC-VIEW-032: Спроба доступу до ресурсу іншого користувача

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-007                  |
| **Issue**    | —                       |
| **TS**       | —                       |
| **Метод**    | Integration (Supertest) |
| **Техніка**  | Security Testing        |
| **Priority** | P1                      |

**Кроки:**

1. Авторизуватися під Користувачем А.
2. Спробувати виконати операцію читання/редагування/видалення ресурсу, що належить Користувачу Б (надіслати запит до ендпоінту з ID ресурсу Користувача Б).
3. Перевірити статус-код відповіді.

**Очікуваний результат:**

- Статус: `403 Forbidden`
- Доступ заборонено, оскільки користувач не є власником ресурсу.

---

### [ ] TC-VIEW-033: Спроба доступу до неіснуючого ресурсу

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-007                  |
| **Issue**    | —                       |
| **TS**       | —                       |
| **Метод**    | Integration (Supertest) |
| **Техніка**  | Error Guessing          |
| **Priority** | P2                      |

**Кроки:**

1. Авторизуватися в системі.
2. Спробувати надіслати запит для отримання, оновлення або видалення ресурсу модуля Views з неіснуючим ID (наприклад, `00000000-0000-0000-0000-000000000000` або `999999`).
3. Перевірити статус-код відповіді.

**Очікуваний результат:**

- Статус: `404 Not Found`
- Body містить повідомлення про те, що ресурс не знайдено.
