# Unit Tests: Views

### [ ] TC-VIEW-U-001: ViewService.create — обмеження ліміту у 10 подань на одну базу даних

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-042                  |
| **Issue**    | #66                     |
| **TS**       | —                       |
| **Метод**    | Unit (Jest)             |
| **Техніка**  | Boundary Value Analysis |
| **Priority** | P2                      |

**Кроки:**

1. База даних вже містить 10 подань.
2. Спробувати створити 11-те подання (`ViewService.create()`).

**Очікуваний результат:**

- Спроба створити 11-те подання відхиляється із `BadRequestException` через перевищення ліміту у 10 подань на одну базу даних.

---

---

### [ ] TC-VIEW-U-002: ViewService.delete — заборона видалення останнього подання бази даних

| Поле         | Значення                             |
| ------------ | ------------------------------------ |
| **US**       | US-042                               |
| **Issue**    | #66                                  |
| **TS**       | —                                    |
| **Метод**    | Unit (Jest)                          |
| **Техніка**  | Equivalence Partitioning (protected) |
| **Priority** | P2                                   |

**Кроки:**

1. База даних містить лише 1 подання.
2. Спробувати видалити це подання (`ViewService.delete()`).

**Очікуваний результат:**

- Спроба видалити єдине подання бази даних відхиляється із помилкою `BadRequestException`.

---

---

### [ ] TC-VIEW-U-003: ViewService.duplicate — копіювання конфігурації фільтрів, сортування та групування

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-042             |
| **Issue**    | #66                |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P2                 |

**Кроки:**

1. Викликати `ViewService.duplicate()` для подання, що має налаштовані фільтри, сортування за 2-ма полями та групування за статусом.
2. Перевірити конфігурацію дубльованого подання.

**Очікуваний результат:**

- Створюється нове подання.
- Усі налаштування фільтрації, сортування та групування повністю ідентичні оригінальному поданню.

---

---

### [ ] TC-VIEW-U-004: ViewQueryBuilder.buildQuery — застосування фільтрів з логікою AND / OR

| Поле         | Значення                 |
| ------------ | ------------------------ |
| **US**       | US-044                   |
| **Issue**    | #68                      |
| **TS**       | —                        |
| **Метод**    | Unit (Jest)              |
| **Техніка**  | Equivalence Partitioning |
| **Priority** | P1                       |

**Кроки:**

1. Створити конфігурацію фільтру з логікою `AND` (`Status = Closed` AND `Profit > 100`).
2. Створити конфігурацію фільтру з логікою `OR` (`Status = Open` OR `Profit > 500`).
3. Викликати `ViewQueryBuilder.buildQuery()` для кожної конфігурації та перевірити згенеровані умови для бази даних.

**Очікуваний результат:**

- Для логіки `AND` генерується SQL/ORM запис, де умови об'єднані через `AND`.
- Для логіки `OR` генерується SQL/ORM запис, де умови об'єднані через `OR`.

---

---

### [ ] TC-VIEW-U-005: ViewQueryBuilder.buildQuery — багатоступеневе сортування за кількома властивостями та мета-полями

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-008             |
| **Issue**    | #32                |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P2                 |

**Кроки:**

1. Створити конфігурацію сортування: 1-й пріоритет — `createdAt DESC`, 2-й пріоритет — `Profit ASC`.
2. Викликати `ViewQueryBuilder.buildQuery()` з цією конфігурацією.
3. Перевірити згенероване сортування (Order Clause).

**Очікуваний результат:**

- Запит містить сортування за обома полями у визначеному порядку та з відповідними напрямками.

---

---

### [ ] TC-VIEW-U-006: ViewGroupingService.group — групування за DATE з вибором деталізації (Місяць / День / Квартал)

| Поле         | Значення                 |
| ------------ | ------------------------ |
| **US**       | US-039                   |
| **Issue**    | #34                      |
| **TS**       | —                        |
| **Метод**    | Unit (Jest)              |
| **Техніка**  | Equivalence Partitioning |
| **Priority** | P2                       |

**Кроки:**

1. Передати список записів із датами у `ViewGroupingService.group()` з типом групування `DATE` та деталізацією `Month`.
2. Передати записи у `ViewGroupingService.group()` з деталізацією `Day`.
3. Перевірити групування записів без вказаної дати.

**Очікуваний результат:**

- При деталізації `Month` записи групуються за місяцями (наприклад, `"June 2026"`).
- При деталізації `Day` записи групуються за днями (наприклад, `"04.06.2026"`).
- Записи без дати потрапляють у спеціальну групу `"Без дати"`.

---

---

### [ ] TC-VIEW-U-007: ViewService.update — ігнорування змін фільтрації/сортування при заблокованому поданні (locked)

| Поле         | Значення                             |
| ------------ | ------------------------------------ |
| **US**       | US-042                               |
| **Issue**    | #66                                  |
| **TS**       | —                                    |
| **Метод**    | Unit (Jest)                          |
| **Техніка**  | Equivalence Partitioning (protected) |
| **Priority** | P2                                   |

**Кроки:**

1. Встановити прапорець `isLocked: true` на поданні.
2. Спробувати оновити налаштування фільтрації або сортування для цього подання.

**Очікуваний результат:**

- Спроба оновити конфігурацію заблокованого подання повертає помилку `ForbiddenException` або `BadRequestException`, запобігаючи випадковим змінам.

---

---

### [ ] TC-VIEW-U-008: ViewSearchService.search — мінімальний поріг пошуку у 2 символи

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-019                  |
| **Issue**    | #18                     |
| **TS**       | —                       |
| **Метод**    | Unit (Jest)             |
| **Техніка**  | Boundary Value Analysis |
| **Priority** | P1                      |

**Кроки:**

1. Викликати `ViewSearchService.search()` з пошуковим запитом довжиною 1 символ (наприклад `"A"`).
2. Викликати `ViewSearchService.search()` з пошуковим запитом довжиною 2 символи (`"AB"`).

**Очікуваний результат:**

- При 1 символі пошук не виконується (повертається загальний список або пустий фільтр).
- При 2 і більше символах запит застосовується, і база даних фільтрується за збігом у назві чи значеннях властивостей.

---

---

---

### [ ] TC-VIEW-U-009: ViewService — Відкат транзакції при виникненні помилки

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-007             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Налаштувати мок репозиторію або Prisma клієнта на успішне виконання першої частини транзакції (наприклад, створення запису).
2. Налаштувати мок репозиторію так, щоб наступний крок транзакції або супутня дія викликали виняток/помилку.
3. Викликати відповідний метод сервісу/usecase в транзакції.
4. Перевірити, що всі зміни, виконані в межах транзакції, відкочуються і помилка прокидається вище.

**Очікуваний результат:**

- Транзакція відміняється (rollback).
- Жодні проміжні зміни не зберігаються в базі даних.
- Метод прокидає відповідну помилку.

---

---

### [ ] TC-VIEW-U-010: ViewService — Обмеження доступу до ресурсу, який не належить користувачу

| Поле         | Значення        |
| ------------ | --------------- |
| **US**       | US-007          |
| **Issue**    | —               |
| **TS**       | —               |
| **Метод**    | Unit (Jest)     |
| **Техніка**  | Branch Coverage |
| **Priority** | P1              |

**Кроки:**

1. Налаштувати мок репозиторію для повернення ресурсу, де `ownerId` відрізняється від ID поточного користувача.
2. Викликати метод сервісу для доступу або модифікації цього ресурсу від імені поточного користувача.
3. Перевірити, що метод викидає `ForbiddenException`.

**Очікуваний результат:**

- Метод повертає `ForbiddenException` та не виконує операцію.

---

---

### [ ] TC-VIEW-U-011: ViewService — Обробка помилки дублювання унікальних полів

| Поле         | Значення       |
| ------------ | -------------- |
| **US**       | US-007         |
| **Issue**    | —              |
| **TS**       | —              |
| **Метод**    | Unit (Jest)    |
| **Техніка**  | Error Guessing |
| **Priority** | P1             |

**Кроки:**

1. Налаштувати мок репозиторію для імітації порушення унікального обмеження у БД (наприклад, унікального імені чи email).
2. Викликати відповідний метод створення або оновлення сервісу.
3. Перевірити, що метод перехоплює помилку репозиторію та викидає `ConflictException`.

**Очікуваний результат:**

- Метод повертає `ConflictException` із повідомленням про помилку дублювання.
