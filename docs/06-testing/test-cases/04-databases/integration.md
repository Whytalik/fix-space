# Integration Tests: Databases

### [x] TC-DB-001: Пресетні бази при ініціалізації

| Поле         | Значення                 |
| ------------ | ------------------------ |
| **US**       | US-024                   |
| **Issue**    | #6                       |
| **TS**       | TS-AUTH-01               |
| **Метод**    | Integration (Supertest)  |
| **Техніка**  | Equivalence Partitioning |
| **Priority** | P1                       |

**Кроки:**

1. Зареєструвати нового користувача, верифікувати, увійти
2. `GET /databases?spaceId=<space-id>`
3. Перевірити кількість та назви БД

**Очікуваний результат:**

- 9 пресетних БД: Trading Journal, Daily Routine, Routine Library, Mistakes, Notes, Accounts, Operations, Trading System, Performance Review
- Кожна має коректні властивості згідно SRS §2.3

---

### [x] TC-DB-002: Пресетні зв'язки між базами

| Поле         | Значення                 |
| ------------ | ------------------------ |
| **US**       | US-024                   |
| **Issue**    | #6                       |
| **TS**       | TS-AUTH-01               |
| **Метод**    | Integration (Supertest)  |
| **Техніка**  | Equivalence Partitioning |
| **Priority** | P1                       |

**Кроки:**

1. Виконати TC-DB-001
2. Перевірити RELATION-властивості в Trading Journal (Account → Accounts, Trading System → Trading System)
3. Перевірити RELATION-властивості в Daily Routine (Trades → Trading Journal)

**Очікуваний результат:**

- RELATION-поля налаштовані з правильними `relatedEntityId`
- Зв'язки між БД коректні

---

### [x] TC-DB-003: Створення кастомної бази даних

| Поле         | Значення                         |
| ------------ | -------------------------------- |
| **US**       | US-020                           |
| **Issue**    | #32                              |
| **TS**       | TS-DB-01                         |
| **Метод**    | Integration (Supertest)          |
| **Техніка**  | Equivalence Partitioning (valid) |
| **Priority** | P1                               |

**Кроки:**

1. Увійти в систему
2. `POST /databases` з тілом:
   ```json
   { "spaceId": "<space-id>", "name": "My Custom DB" }
   ```
3. Перевірити статус-код та тіло відповіді

**Очікуваний результат:**

- Статус: `201 Created`
- БД створена з мінімальним набором (властивість Name)
- Іконка за замовчуванням присвоєна

---

### [x] TC-DB-004: Створення БД з порожньою назвою

| Поле         | Значення                           |
| ------------ | ---------------------------------- |
| **US**       | US-020                             |
| **Issue**    | #32                                |
| **TS**       | TS-DB-01                           |
| **Метод**    | Integration (Supertest)            |
| **Техніка**  | Equivalence Partitioning (invalid) |
| **Priority** | P1                                 |

**Кроки:**

1. `POST /databases` з тілом: `{ "spaceId": "<space-id>", "name": "" }`
2. Перевірити статус-код

**Очікуваний результат:**

- Статус: `400 Bad Request`
- Body містить помилку валідації поля `name`

---

### [x] TC-DB-005: Створення БД з дублікатом назви

| Поле         | Значення                             |
| ------------ | ------------------------------------ |
| **US**       | US-020                               |
| **Issue**    | #32                                  |
| **TS**       | TS-DB-01                             |
| **Метод**    | Integration (Supertest)              |
| **Техніка**  | Equivalence Partitioning (duplicate) |
| **Priority** | P1                                   |

**Кроки:**

1. Створити БД "My DB"
2. `POST /databases` з тілом: `{ "spaceId": "<space-id>", "name": "My DB" }`
3. Перевірити статус-код

**Очікуваний результат:**

- Статус: `409 Conflict`
- Body: повідомлення про дублікат назви в межах простору

---

### [x] TC-DB-006: Перейменування бази даних

| Поле         | Значення                 |
| ------------ | ------------------------ |
| **US**       | US-020                   |
| **Issue**    | #33                      |
| **TS**       | TS-DB-01                 |
| **Метод**    | Integration (Supertest)  |
| **Техніка**  | Equivalence Partitioning |
| **Priority** | P1                       |

**Кроки:**

1. Створити БД "Old Name"
2. `PATCH /databases/:id` з тілом: `{ "name": "New Name" }`
3. Перевірити що назва оновлена в sidebar, заголовку, RELATION-полях

**Очікуваний результат:**

- Статус: `200 OK`
- Назва "New Name" відображається всюди

---

### [x] TC-DB-007: Перейменування — унікальність

| Поле         | Значення                             |
| ------------ | ------------------------------------ |
| **US**       | US-020                               |
| **Issue**    | #33                                  |
| **TS**       | TS-DB-01                             |
| **Метод**    | Integration (Supertest)              |
| **Техніка**  | Equivalence Partitioning (duplicate) |
| **Priority** | P1                                   |

**Кроки:**

1. Маємо БД "DB-1" та "DB-2"
2. `PATCH /databases/:db2-id` з тілом: `{ "name": "DB-1" }`
3. Перевірити статус-код

**Очікуваний результат:**

- Статус: `409 Conflict`

---

### [x] TC-DB-008: Видалення кастомної БД

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-020                  |
| **Issue**    | #34                     |
| **TS**       | TS-DB-01                |
| **Метод**    | Integration (Supertest) |
| **Техніка**  | State Transition        |
| **Priority** | P1                      |

**Кроки:**

1. Створити кастомну БД
2. `DELETE /databases/:id`
3. `GET /databases/:id` — перевірити що не існує

**Очікуваний результат:**

- Статус: `200 OK`
- БД видалена, всі записи видалені каскадно

---

### [x] TC-DB-009: Видалення пресетної БД

| Поле         | Значення                 |
| ------------ | ------------------------ |
| **US**       | US-020                   |
| **Issue**    | #34                      |
| **TS**       | TS-DB-01                 |
| **Метод**    | Integration (Supertest)  |
| **Техніка**  | Equivalence Partitioning |
| **Priority** | P1                       |

**Кроки:**

1. Отримати ID пресетної БД (Trading Journal)
2. `DELETE /databases/:id`
3. Перевірити статус-код

**Очікуваний результат:**

- Статус: `200 OK`
- БД видалена, всі записи видалені каскадно
- _Примітка: попередження про системну базу та залежності показується на фронтенді перед відправкою запиту_

---

### [x] TC-DB-010: Видалення чужої БД

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-020                  |
| **Issue**    | #34                     |
| **TS**       | TS-DB-01                |
| **Метод**    | Integration (Supertest) |
| **Техніка**  | Security Test           |
| **Priority** | P1                      |

**Кроки:**

1. Користувач A створює БД
2. Користувач B намагається `DELETE /databases/:id`
3. Перевірити статус-код

**Очікуваний результат:**

- Статус: `403 Forbidden`

---

### [x] TC-DB-011: Блокування структури БД

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-032                  |
| **Issue**    | #56                     |
| **TS**       | —                       |
| **Метод**    | Integration (Supertest) |
| **Техніка**  | State Transition        |
| **Priority** | P2                      |

**Кроки:**

1. Створити БД
2. `PATCH /databases/:id` з тілом: `{ "isLocked": true }`
3. Спробувати додати властивість: `POST /properties` з `databaseId`
4. Перевірити статус-код

**Очікуваний результат:**

- Статус блокування: `200 OK`
- Додавання властивості: `403 Forbidden`
- Body: повідомлення що структура заблокована

---

### [x] TC-DB-012: Введення даних у заблоковану БД

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-032                  |
| **Issue**    | #56                     |
| **TS**       | —                       |
| **Метод**    | Integration (Supertest) |
| **Техніка**  | State Transition        |
| **Priority** | P2                      |

**Кроки:**

1. Заблокувати БД (TC-DB-011)
2. Створити запис: `POST /records` з `databaseId`
3. Перевірити статус-код

**Очікуваний результат:**

- Статус: `201 Created`
- Запис створений (введення даних дозволене)

---

### [x] TC-DB-013: Розблокування структури БД

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-032                  |
| **Issue**    | #56                     |
| **TS**       | —                       |
| **Метод**    | Integration (Supertest) |
| **Техніка**  | State Transition        |
| **Priority** | P2                      |

**Кроки:**

1. Заблокувати БД
2. `PATCH /databases/:id` з тілом: `{ "isLocked": false }`
3. Додати властивість — перевірити що дозволено

**Очікуваний результат:**

- Статус: `200 OK`
- Додавання властивості: `201 Created` (структура розблокована)

---

### [x] TC-DB-014: Дублювання БД

| Поле         | Значення                 |
| ------------ | ------------------------ |
| **US**       | US-049                   |
| **Issue**    | #76                      |
| **TS**       | TS-DB-02                 |
| **Метод**    | Integration (Supertest)  |
| **Техніка**  | Equivalence Partitioning |
| **Priority** | P2                       |

**Кроки:**

1. Створити БД з властивостями та шаблонами
2. `POST /databases/:id/duplicate`
3. Перевірити що нова БД має ті самі властивості та шаблони

**Очікуваний результат:**

- Статус: `201 Created`
- Нова БД має копію властивостей та шаблонів
- Записи НЕ скопійовані

---

### [x] TC-DB-015: Дублювання — назва з суфіксом

| Поле         | Значення                 |
| ------------ | ------------------------ |
| **US**       | US-049                   |
| **Issue**    | #76                      |
| **TS**       | TS-DB-02                 |
| **Метод**    | Integration (Supertest)  |
| **Техніка**  | Equivalence Partitioning |
| **Priority** | P2                       |

**Кроки:**

1. Дублювати БД "Trading Journal"
2. Перевірити назву нової БД

**Очікуваний результат:**

- Назва: "Trading Journal (copy)" або аналогічна унікальна

---

### [x] TC-DB-016: Дублювання — в тому самому розділі

| Поле         | Значення                 |
| ------------ | ------------------------ |
| **US**       | US-049                   |
| **Issue**    | #76                      |
| **TS**       | TS-DB-02                 |
| **Метод**    | Integration (Supertest)  |
| **Техніка**  | Equivalence Partitioning |
| **Priority** | P2                       |

**Кроки:**

1. БД в розділі "Trading"
2. Дублювати БД
3. Перевірити що копія в тому самому розділі

**Очікуваний результат:**

- Копія БД має той самий `sectionId`

---

### [x] TC-DB-024: Запит до захищеного ендпоінту без токена автентифікації

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-020                  |
| **Issue**    | —                       |
| **TS**       | —                       |
| **Метод**    | Integration (Supertest) |
| **Техніка**  | Security Testing        |
| **Priority** | P1                      |

**Кроки:**

1. Надіслати запит до будь-якого захищеного ендпоінту модуля Databases (наприклад, `GET /databases`) без заголовка `Authorization`.
2. Перевірити статус-код відповіді та повідомлення про помилку.

**Очікуваний результат:**

- Статус: `401 Unauthorized`
- Body містить повідомлення про відсутність або невалідність токена.

---

### [x] TC-DB-025: Спроба доступу до неіснуючого ресурсу

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-020                  |
| **Issue**    | —                       |
| **TS**       | —                       |
| **Метод**    | Integration (Supertest) |
| **Техніка**  | Error Guessing          |
| **Priority** | P2                      |

**Кроки:**

1. Авторизуватися в системі.
2. Спробувати надіслати запит для отримання, оновлення або видалення ресурсу модуля Databases з неіснуючим ID (наприклад, `00000000-0000-0000-0000-000000000000` або `999999`).
3. Перевірити статус-код відповіді.

**Очікуваний результат:**

- Статус: `404 Not Found`
- Body містить повідомлення про те, що ресурс не знайдено.

---

### [x] TC-DB-026: GET /databases?spaceId=... — повертає список баз даних

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-020                  |
| **Issue**    | —                       |
| **TS**       | —                       |
| **Метод**    | Integration (Supertest) |
| **Техніка**  | Statement Coverage      |
| **Priority** | P1                      |

**Кроки:**

1. Зареєструвати та верифікувати користувача, увійти.
2. Отримати id простору через `GET /spaces`.
3. Надіслати `GET /databases?spaceId=<id>`.

**Очікуваний результат:**

- Статус `200 OK`.
- Тіло — масив об'єктів `DatabaseResponseDto`.
- Масив містить ініціалізовані пресетні бази даних.

---

### [x] TC-DB-027: GET /databases/:id — повертає конкретну базу даних

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-020                  |
| **Issue**    | —                       |
| **TS**       | —                       |
| **Метод**    | Integration (Supertest) |
| **Техніка**  | Statement Coverage      |
| **Priority** | P1                      |

**Кроки:**

1. Зареєструвати та верифікувати користувача, увійти.
2. Отримати id бази через `GET /databases?spaceId=<id>`.
3. Надіслати `GET /databases/:id`.

**Очікуваний результат:**

- Статус `200 OK`.
- Тіло — `DatabaseResponseDto` з полями `id`, `name`, `icon`, `properties`.
