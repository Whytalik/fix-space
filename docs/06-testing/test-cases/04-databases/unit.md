# Unit Tests: Databases

### [x] TC-DB-U-001: DatabaseService.create — Створення бази даних з властивостями за замовчуванням

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-020             |
| **Issue**    | #32                |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Налаштувати репозиторій на повернення простору для `spaceId` та `userId`
2. Викликати `DatabaseService.create(spaceId, { name: "Custom DB", properties: [{ name: "Name", type: "TEXT" }] }, userId)`
3. Перевірити, що `DatabaseRepository.create` викликається з коректними даними
4. Перевірити, що для властивості `"Name"` створюється конфігурація за допомогою `PropertyTypeRegistry`

**Очікуваний результат:**

- База даних створюється у транзакції
- Конфігурація властивості `"Name"` згенерована та збережена
- Повертається створений `DatabaseResponseDto`

---

---

### [x] TC-DB-U-002: DatabaseService.create — Валідація простору та унікальності назви бази

| Поле         | Значення        |
| ------------ | --------------- |
| **US**       | US-020          |
| **Issue**    | #32             |
| **TS**       | —               |
| **Метод**    | Unit (Jest)     |
| **Техніка**  | Branch Coverage |
| **Priority** | P1              |

**Кроки:**

1. Викликати `create` з неіснуючим `spaceId` (налаштувати репозиторій на повернення `null`)
2. Перевірити викидання `NotFoundException`
3. Налаштувати репозиторій на повернення простору, але дубльованої назви (`findByNameInSpace` повертає `true`)
4. Викликати `create` з дубльованою назвою
5. Перевірити викидання `ConflictException`

**Очікуваний результат:**

- Неіснуючий простір → `NotFoundException`
- Зайнята назва БД в просторі → `ConflictException`

---

---

### [x] TC-DB-U-003: DatabaseService.findOne — Помилка у разі відсутності бази даних

| Поле         | Значення        |
| ------------ | --------------- |
| **US**       | US-020          |
| **Issue**    | #32             |
| **TS**       | —               |
| **Метод**    | Unit (Jest)     |
| **Техніка**  | Branch Coverage |
| **Priority** | P1              |

**Кроки:**

1. Налаштувати `DatabaseRepository.findById` на повернення `null`
2. Викликати `DatabaseService.findOne(id)`
3. Перевірити викидання `NotFoundException`

**Очікуваний результат:**

- Виклик `findOne` повертає `NotFoundException` із повідомленням `errors.DATABASE_NOT_FOUND_ID`

---

---

### [x] TC-DB-U-004: DatabaseService.update — Оновлення та перевірка приналежності секції до простору

| Поле         | Значення        |
| ------------ | --------------- |
| **US**       | US-020          |
| **Issue**    | #33             |
| **TS**       | —               |
| **Метод**    | Unit (Jest)     |
| **Техніка**  | Branch Coverage |
| **Priority** | P1              |

**Кроки:**

1. Налаштувати репозиторій на повернення бази даних для `id`
2. Налаштувати репозиторій так, щоб `findSectionInSpace` повертав `null` для вказаного `sectionId`
3. Викликати `DatabaseService.update(id, { sectionId: "invalid-section-id" })`
4. Перевірити викидання `NotFoundException`
5. Змінити `findSectionInSpace` на повернення секції, викликати `update` з валідними даними, перевірити збереження оновлень

**Очікуваний результат:**

- Невалідний `sectionId` → `NotFoundException`
- Валідні дані → оновлення через `DatabaseRepository.update` та повернення `DatabaseResponseDto`

---

---

### [x] TC-DB-U-005: DatabaseService.remove — Видалення бази даних та обробка винятків

| Поле         | Значення        |
| ------------ | --------------- |
| **US**       | US-020          |
| **Issue**    | #34             |
| **TS**       | —               |
| **Метод**    | Unit (Jest)     |
| **Техніка**  | Branch Coverage |
| **Priority** | P1              |

**Кроки:**

1. Викликати `DatabaseService.remove(id)`, коли бази даних не існує
2. Перевірити викидання `NotFoundException`
3. Викликати `DatabaseService.remove(id)` з існуючою базою даних
4. Перевірити виклик `DatabaseRepository.delete`

**Очікуваний результат:**

- Відсутня БД → `NotFoundException`
- Існуюча БД → успішно видаляється та повертається `DatabaseResponseDto`

---

---

### [x] TC-DB-U-006: DuplicateDatabaseUseCase — Повне дублювання структури, записів та значень

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-020             |
| **Issue**    | #32                |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Налаштувати репозиторій на повернення вихідної БД з властивостями, записами та значеннями
2. Викликати `DuplicateDatabaseUseCase.execute(databaseId)`
3. Перевірити створення копії БД з новим іменем та унікальним slug
4. Перевірити, що всі властивості, записи та їх значення переносяться, а ідентифікатори властивостей мапуються коректно

**Очікуваний результат:**

- База даних успішно дублюється у транзакції
- Унікальні назви та slug згенеровані
- Копії властивостей, записів та значень мають правильні прив'язки до нової бази та нових властивостей

---

---

---

### [x] TC-DB-U-007: DatabaseService — Відкат транзакції при виникненні помилки

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-020             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Налаштувати мок репозиторію або Prisma клієнта на успішне виконання першої частини транзакції (наприклад, створення запису).
2. Налаштувати мок репозиторію так, щоб наступний крок транзакції або супутня дія викликали виняток/помилку.
3. Викликати відповідний метод сервісу/usecase в транзакції.
4. Перевірити, що всі зміни, виконані в межах транзакції, відкочуються і помилка прокидається вище.

**Очікуваний результат:**

- Транзакція відміняється (rollback).
- Жодні проміжні зміни не зберігаються в базі даних.
- Метод прокидає відповідну помилку.

---

---

### [x] TC-DB-U-008: DatabaseService — Обмеження доступу до ресурсу, який не належить користувачу

| Поле         | Значення        |
| ------------ | --------------- |
| **US**       | US-020          |
| **Issue**    | —               |
| **TS**       | —               |
| **Метод**    | Unit (Jest)     |
| **Техніка**  | Branch Coverage |
| **Priority** | P1              |

**Кроки:**

1. Налаштувати мок репозиторію для повернення ресурсу, де `ownerId` відрізняється від ID поточного користувача.
2. Викликати метод сервісу для доступу або модифікації цього ресурсу від імені поточного користувача.
3. Перевірити, що метод викидає `ForbiddenException`.

**Очікуваний результат:**

- Метод повертає `ForbiddenException` та не виконує операцію.

---

### [x] TC-DB-U-009: DatabaseService.findAll — повертає список баз даних простору

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-020             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Мок `databaseRepo.findAll` повертає масив із двох баз даних.
2. Викликати `DatabaseService.findAll("space-1", "user-1")`.

**Очікуваний результат:**

- Повертається масив із двох `DatabaseResponseDto`.
- Дані фільтруються за `spaceId` та `userId`.
