# Unit Tests: Buttons

### [ ] TC-BTN-U-001: ButtonExecutionEngine — виконання ланцюжка дій та обробка помилок

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-109             |
| **Issue**    | #61                |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P2                 |

**Кроки:**

1. Налаштувати кнопку з 3 діями: 1) `setFieldValue` (Status = Closed), 2) `createRecord` (Mistakes), 3) `setFieldValue` (Reviewed = true).
2. Запустити виконання кнопки.
3. Запустити виконання кнопки, де дія `createRecord` завершується помилкою через недійсні права або ліміти.

**Очікуваний результат:**

- Крок 2: усі 3 дії виконано успішно одна за одною.
- Крок 3: 1-ша дія виконана, на 2-й виконується зупинка із викиданням винятку, 3-тя дія не виконується взагалі.

---

---

### [ ] TC-BTN-U-002: ButtonExecutionEngine — збереження метаданих виконання у ButtonExecution

| Поле         | Значення         |
| ------------ | ---------------- |
| **US**       | US-108           |
| **Issue**    | #61              |
| **TS**       | —                |
| **Метод**    | Unit (Jest)      |
| **Техніка**  | State Transition |
| **Priority** | P2               |

**Кроки:**

1. Натиснути кнопку на записі та дочекатися завершення виконання дій.
2. Перевірити збережений запис `ButtonExecution` в системі.
3. Спробувати відфільтрувати або відсортувати записи по полю `executedAt` метаданих кнопки.

**Очікуваний результат:**

- Крок 2: у системі збережено метадані: `recordId`, `propertyId`, `executedAt` (ISO timestamp).
- Крок 3: система не дозволяє використовувати метадані кнопки для фільтрації чи сортування.

---

---

### [ ] TC-BTN-U-003: ButtonValidator — перевірка обмежень властивості Button

| Поле         | Значення        |
| ------------ | --------------- |
| **US**       | US-108          |
| **Issue**    | #61             |
| **TS**       | —               |
| **Метод**    | Unit (Jest)     |
| **Техніка**  | Branch Coverage |
| **Priority** | P3              |

**Кроки:**

1. Передати властивість BUTTON у метод побудови сортування чи фільтрування бази даних.
2. Передати властивість BUTTON у список полів для масового редагування (bulk edit).

**Очікуваний результат:**

- Крок 1: викидається виняток `BadRequestException` (властивості BUTTON не підтримують фільтрацію/сортування).
- Крок 2: викидається виняток `BadRequestException` (масове редагування кнопок заборонене).

---

---

### [ ] TC-BTN-U-004: ButtonExecutionEngine — виконання дії "Вставити блок у контент" (insertBlock)

| Поле         | Значення          |
| ------------ | ----------------- |
| **US**       | US-110            |
| **Issue**    | #62               |
| **TS**       | —                 |
| **Метод**    | Unit (Jest)       |
| **Техніка**  | Equivalence Part. |
| **Priority** | P2                |

**Кроки:**

1. Запустити дію кнопки `insertBlock` із параметром `position = after_last`.
2. Запустити дію кнопки `insertBlock` із параметром `position = before_first`.
3. Вставити блок із увімкненою опцією підстановки мітки часу у назву блоку.

**Очікуваний результат:**

- Крок 1: новий контентний блок вставляється в самий кінець контентної області запису.
- Крок 2: новий контентний блок вставляється на першу позицію контентної області.
- Крок 3: вставлений блок має назву із поточною датою/часом, наприклад, `"Daily Log - 2026-06-04"`.

---

---

### [ ] TC-BTN-U-005: ButtonValidator — перевірка конфліктів та валідація дій конфігурації

| Поле         | Значення        |
| ------------ | --------------- |
| **US**       | US-111          |
| **Issue**    | #118            |
| **TS**       | —               |
| **Метод**    | Unit (Jest)     |
| **Техніка**  | Branch Coverage |
| **Priority** | P2              |

**Кроки:**

1. Створити кнопку, дія якої намагається встановити значення іншої BUTTON-властивості.
2. Створити кнопку, дія якої намагається змінити значення FORMULA-властивості.
3. Створити кнопку, що посилається на звичайне текстове або числове поле (валідний шлях).

**Очікуваний результат:**

- Крок 1: викидається виняток `BadRequestException` (неможливо посилатися на кнопки у діях).
- Крок 2: викидається виняток `BadRequestException` (неможливо редагувати формули).
- Крок 3: конфігурація проходить валідацію успішно.

---

---

---

### [ ] TC-BTN-U-006: ButtonService — Відкат транзакції при виникненні помилки

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-108             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Налаштувати мок репозиторію або Prisma клієнта на успішне виконання першої частини транзакції (наприклад, створення запису).
2. Налаштувати мок репозиторію так, щоб наступний крок транзакції або супутня дія викликали виняток/помилку.
3. Викликати відповідний метод сервісу/usecase в транзакції.
4. Перевірити, що всі зміни, виконані в межах транзакції, відкочуються і помилка прокидається вище.

**Очікуваний результат:**

- Транзакція відміняється (rollback).
- Жодні проміжні зміни не зберігаються в базі даних.
- Метод прокидає відповідну помилку.

---

---

### [ ] TC-BTN-U-007: ButtonService — Обмеження доступу до ресурсу, який не належить користувачу

| Поле         | Значення        |
| ------------ | --------------- |
| **US**       | US-108          |
| **Issue**    | —               |
| **TS**       | —               |
| **Метод**    | Unit (Jest)     |
| **Техніка**  | Branch Coverage |
| **Priority** | P1              |

**Кроки:**

1. Налаштувати мок репозиторію для повернення ресурсу, де `ownerId` відрізняється від ID поточного користувача.
2. Викликати метод сервісу для доступу або модифікації цього ресурсу від імені поточного користувача.
3. Перевірити, що метод викидає `ForbiddenException`.

**Очікуваний результат:**

- Метод повертає `ForbiddenException` та не виконує операцію.

---

---

### [ ] TC-BTN-U-008: ButtonService — Обробка помилки дублювання унікальних полів

| Поле         | Значення       |
| ------------ | -------------- |
| **US**       | US-108         |
| **Issue**    | —              |
| **TS**       | —              |
| **Метод**    | Unit (Jest)    |
| **Техніка**  | Error Guessing |
| **Priority** | P1             |

**Кроки:**

1. Налаштувати мок репозиторію для імітації порушення унікального обмеження у БД (наприклад, унікального імені чи email).
2. Викликати відповідний метод створення або оновлення сервісу.
3. Перевірити, що метод перехоплює помилку репозиторію та викидає `ConflictException`.

**Очікуваний результат:**

- Метод повертає `ConflictException` із повідомленням про помилку дублювання.
