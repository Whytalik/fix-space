# Integration Tests: Properties

### [x] TC-PROP-001: Створення властивості — TEXT

| Поле         | Значення                         |
| ------------ | -------------------------------- |
| **US**       | US-011                           |
| **Issue**    | #9                               |
| **TS**       | TS-PROP-01                       |
| **Метод**    | Integration (Supertest)          |
| **Техніка**  | Equivalence Partitioning (valid) |
| **Priority** | P1                               |

**Кроки:**

1. Увійти в систему, отримати databaseId
2. `POST /properties` з тілом:
   ```json
   {
     "databaseId": "<db-id>",
     "name": "Notes",
     "type": "TEXT"
   }
   ```
3. Перевірити статус-код

**Очікуваний результат:**

- Статус: `201 Created`
- Властивість створена з типом TEXT

---

### [x] TC-PROP-002: Створення властивості — NUMBER

| Поле         | Значення                         |
| ------------ | -------------------------------- |
| **US**       | US-011                           |
| **Issue**    | #9                               |
| **TS**       | TS-PROP-01                       |
| **Метод**    | Integration (Supertest)          |
| **Техніка**  | Equivalence Partitioning (valid) |
| **Priority** | P1                               |

**Кроки:**

1. `POST /properties` з тілом:
   ```json
   {
     "databaseId": "<db-id>",
     "name": "Price",
     "type": "NUMBER",
     "config": { "format": "currency", "decimalPlaces": 2 }
   }
   ```
2. Перевірити статус-код

**Очікуваний результат:**

- Статус: `201 Created`
- Властивість створена з типом NUMBER та конфігурацією

---

### [x] TC-PROP-003: Створення властивості з дублікатом назви

| Поле         | Значення                             |
| ------------ | ------------------------------------ |
| **US**       | US-011                               |
| **Issue**    | #9                                   |
| **TS**       | TS-PROP-01                           |
| **Метод**    | Integration (Supertest)              |
| **Техніка**  | Equivalence Partitioning (duplicate) |
| **Priority** | P1                                   |

**Кроки:**

1. Створити властивість "Price"
2. `POST /properties` з тілом: `{ "databaseId": "<db-id>", "name": "Price", "type": "NUMBER" }`
3. Перевірити статус-код

**Очікуваний результат:**

- Статус: `409 Conflict`
- Body: повідомлення про дублікат назви в межах БД

---

### [x] TC-PROP-004: Редагування назви властивості

| Поле         | Значення                 |
| ------------ | ------------------------ |
| **US**       | US-012                   |
| **Issue**    | #10                      |
| **TS**       | TS-PROP-01               |
| **Метод**    | Integration (Supertest)  |
| **Техніка**  | Equivalence Partitioning |
| **Priority** | P1                       |

**Кроки:**

1. Створити властивість "Old Name"
2. `PATCH /properties/:id` з тілом: `{ "name": "New Name" }`
3. Перевірити що назва оновлена у всіх записах

**Очікуваний результат:**

- Статус: `200 OK`
- Назва "New Name" відображається у всіх записах

---

### [x] TC-PROP-005: Редагування параметрів типу

| Поле         | Значення                 |
| ------------ | ------------------------ |
| **US**       | US-012                   |
| **Issue**    | #10                      |
| **TS**       | TS-PROP-01               |
| **Метод**    | Integration (Supertest)  |
| **Техніка**  | Equivalence Partitioning |
| **Priority** | P1                       |

**Кроки:**

1. Створити NUMBER властивість з `decimalPlaces: 2`
2. `PATCH /properties/:id` з тілом: `{ "config": { "decimalPlaces": 4 } }`
3. Перевірити що конфігурація оновлена

**Очікуваний результат:**

- Статус: `200 OK`
- Конфігурація оновлена, існуючі значення відображаються з новою точністю

---

### [x] TC-PROP-006: Редагування системної властивості (неможливість зміни типу)

| Поле         | Значення                 |
| ------------ | ------------------------ |
| **US**       | US-012                   |
| **Issue**    | #10                      |
| **TS**       | TS-PROP-01               |
| **Метод**    | Integration (Supertest)  |
| **Техніка**  | Equivalence Partitioning |
| **Priority** | P1                       |

**Кроки:**

1. Отримати ID системної властивості (наприклад, Name в Trading Journal)
2. `PATCH /properties/:id` з тілом: `{ "type": "NUMBER" }` (спроба зміни типу)
3. Перевірити статус-код

**Очікуваний результат:**

- Статус: `403 Forbidden`
- Помилка: зміна типу системної властивості "Name" заборонена

---

### [x] TC-PROP-007: Видалення кастомної властивості

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-023                  |
| **Issue**    | #30                     |
| **TS**       | TS-PROP-01              |
| **Метод**    | Integration (Supertest) |
| **Техніка**  | State Transition        |
| **Priority** | P1                      |

**Кроки:**

1. Створити кастомну властивість
2. Створити запис із значенням цієї властивості
3. `DELETE /properties/:id`
4. Перевірити що властивість видалена, значення в записах видалені

**Очікуваний результат:**

- Статус: `200 OK`
- Властивість видалена з усіх записів

---

### [x] TC-PROP-008: Видалення системної властивості (неможливість видалення)

| Поле         | Значення                 |
| ------------ | ------------------------ |
| **US**       | US-023                   |
| **Issue**    | #30                      |
| **TS**       | TS-PROP-01               |
| **Метод**    | Integration (Supertest)  |
| **Техніка**  | Equivalence Partitioning |
| **Priority** | P1                       |

**Кроки:**

1. Отримати ID системної властивості
2. `DELETE /properties/:id`
3. Перевірити статус-код

**Очікуваний результат:**

- Статус: `403 Forbidden`
- Помилка: видалення системної властивості "Name" заборонено

---

### [x] TC-PROP-009: Видалення — властивість видаляється з фільтрів view

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-023                  |
| **Issue**    | #30                     |
| **TS**       | TS-PROP-01              |
| **Метод**    | Integration (Supertest) |
| **Техніка**  | State Transition        |
| **Priority** | P1                      |

**Кроки:**

1. Створити властивість
2. Видалити властивість
3. Перевірити що властивість видалена та повертає статус-код 404

**Очікуваний результат:**

- Статус: `200 OK` на видалення
- При наступному запиті властивість не знайдено (404)

---

### [x] TC-PROP-010: Зміна порядку властивостей

| Поле         | Значення                 |
| ------------ | ------------------------ |
| **US**       | US-024                   |
| **Issue**    | #31                      |
| **TS**       | TS-PROP-01               |
| **Метод**    | Integration (Supertest)  |
| **Техніка**  | Equivalence Partitioning |
| **Priority** | P1                       |

**Кроки:**

1. Створити властивість
2. `PATCH /properties/:id` з тілом: `{ "position": 0 }`
3. `GET /properties/:id` — перевірити position

**Очікуваний результат:**

- Статус: `200 OK`
- Властивість має `position: 0`

---

### [x] TC-PROP-011: Зміна порядку — збереження між сесіями

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-024                  |
| **Issue**    | #31                     |
| **TS**       | TS-PROP-01              |
| **Метод**    | Integration (Supertest) |
| **Техніка**  | State Transition        |
| **Priority** | P1                      |

**Кроки:**

1. Змінити порядок властивостей (TC-PROP-010)
2. Запитати властивість знову
3. Перевірити position

**Очікуваний результат:**

- Порядок зберігся

---

### [x] TC-PROP-014: Валідація типів при збереженні

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-010                  |
| **Issue**    | #7, #8, #19             |
| **TS**       | TS-PROP-01              |
| **Метод**    | Integration (Supertest) |
| **Техніка**  | Error Guessing          |
| **Priority** | P1                      |

**Кроки:**

1. Створити NUMBER властивість
2. `POST /values` з тілом: `{ "recordId": "<record-id>", "propertyId": "<prop-id>", "value": "not-a-number" }`
3. Перевірити статус-код

**Очікуваний результат:**

- Статус: `400 Bad Request`
- Body: помилка валідації типу

---

### [x] TC-PROP-015: Зміна типу кастомної властивості — сумісна конвертація

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-052                  |
| **Issue**    | #77                     |
| **TS**       | —                       |
| **Метод**    | Integration (Supertest) |
| **Техніка**  | State Transition        |
| **Priority** | P2                      |

**Кроки:**

1. Створити TEXT властивість зі значенням "123"
2. `PATCH /properties/:id` з тілом: `{ "type": "NUMBER" }`
3. Перевірити що значення конвертовано

**Очікуваний результат:**

- Статус: `200 OK`
- Значення "123" конвертовано в число 123

---

### [x] TC-PROP-016: Зміна типу — несумісна конвертація

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-052                  |
| **Issue**    | #77                     |
| **TS**       | —                       |
| **Метод**    | Integration (Supertest) |
| **Техніка**  | Error Guessing          |
| **Priority** | P2                      |

**Кроки:**

1. Створити TEXT властивість зі значенням "hello"
2. `PATCH /properties/:id` з тілом: `{ "type": "NUMBER" }`
3. Перевірити що отримана конвертація за замовчуванням

**Очікуваний результат:**

- Статус: `200 OK`
- Значення конвертовано у значення за замовчуванням для чисел (0)

---

### [x] TC-PROP-017: Зміна типу системної властивості (неможливість зміни)

| Поле         | Значення                 |
| ------------ | ------------------------ |
| **US**       | US-052                   |
| **Issue**    | #77                      |
| **TS**       | —                        |
| **Метод**    | Integration (Supertest)  |
| **Техніка**  | Equivalence Partitioning |
| **Priority** | P2                       |

**Кроки:**

1. Отримати ID системної властивості
2. `PATCH /properties/:id` з тілом: `{ "type": "SELECT" }`
3. Перевірити статус-код

**Очікуваний результат:**

- Статус: `403 Forbidden`
- Помилка: зміна типу системної властивості заборонена

---

### [x] TC-PROP-020: Дублювання властивості

| Поле         | Значення                 |
| ------------ | ------------------------ |
| **US**       | US-054                   |
| **Issue**    | #79                      |
| **TS**       | —                        |
| **Метод**    | Integration (Supertest)  |
| **Техніка**  | Equivalence Partitioning |
| **Priority** | P2                       |

**Кроки:**

1. Створити властивість "Price" з конфігурацією
2. `POST /properties/:id/duplicate`
3. Перевірити назву та конфігурацію копії

**Очікуваний результат:**

- Статус: `201 Created`
- Назва: "Price (copy)", конфігурація скопійована
- Значення записів НЕ скопійовані

---

### [x] TC-PROP-021: Дублювання — значення не копіюються

| Поле         | Значення                 |
| ------------ | ------------------------ |
| **US**       | US-054                   |
| **Issue**    | #79                      |
| **TS**       | —                        |
| **Метод**    | Integration (Supertest)  |
| **Техніка**  | Equivalence Partitioning |
| **Priority** | P2                       |

**Кроки:**

1. Властивість "Price" має значення в записах
2. Дублювати властивість
3. Перевірити що нова властивість має значення за замовчуванням (не скопійовані)

**Очікуваний результат:**

- Значення нової властивості = null (не скопійовані)

---

### [x] TC-PROP-022: Група властивостей — створення

| Поле         | Значення                 |
| ------------ | ------------------------ |
| **US**       | US-055                   |
| **Issue**    | #80                      |
| **TS**       | —                        |
| **Метод**    | Integration (Supertest)  |
| **Техніка**  | Equivalence Partitioning |
| **Priority** | P2                       |

**Кроки:**

1. Створити групу властивостей з назвою "Financials"
2. Додати властивості до групи
3. Перевірити що група створена

**Очікуваний результат:**

- Статус: `201 Created`
- Група створена та повертає назву та ID

---

### [x] TC-PROP-023: Умова видимості групи

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-055                  |
| **Issue**    | #80                     |
| **TS**       | —                       |
| **Метод**    | Integration (Supertest) |
| **Техніка**  | State Transition        |
| **Priority** | P2                      |

**Кроки:**

1. Створити групу з конфігурацією видимості: `{ "type": "Demo" }`
2. Перевірити конфігурацію видимості у створеній групі

**Очікуваний результат:**

- Створена група повертає конфігурацію видимості `visibility: { type: "Demo" }`

---

### [x] TC-PROP-028: Запит до захищеного ендпоінту без токена автентифікації

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-011                  |
| **Issue**    | —                       |
| **TS**       | —                       |
| **Метод**    | Integration (Supertest) |
| **Техніка**  | Security Testing        |
| **Priority** | P1                      |

**Кроки:**

1. Надіслати запит до будь-якого захищеного ендпоінту модуля Properties (наприклад, `GET /properties`) без заголовка `Authorization`.
2. Перевірити статус-код відповіді та повідомлення про помилку.

**Очікуваний результат:**

- Статус: `401 Unauthorized`
- Body містить повідомлення про відсутність або невалідність токена.

---

### [x] TC-PROP-029: Спроба доступу до ресурсу іншого користувача

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-011                  |
| **Issue**    | —                       |
| **TS**       | —                       |
| **Метод**    | Integration (Supertest) |
| **Техніка**  | Security Testing        |
| **Priority** | P1                      |

**Кроки:**

1. Авторизуватися під Користувачем А.
2. Спробувати виконати операцію читання/редагування/видалення ресурсу, що належить Користувачу Б (надіслати запит до ендпоінту з ID ресурсу Користувача Б).
3. Перевірити статус-код відповіді.

**Очікуваний результат:**

- Статус: `403 Forbidden` або `404 Not Found` (якщо ресурс належить до чужої бази даних, яка недоступна)

---

### [x] TC-PROP-030: Спроба доступу до неіснуючого ресурсу

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-011                  |
| **Issue**    | —                       |
| **TS**       | —                       |
| **Метод**    | Integration (Supertest) |
| **Техніка**  | Error Guessing          |
| **Priority** | P2                      |

**Кроки:**

1. Авторизуватися в системі.
2. Спробувати надіслати запит для отримання, оновлення або видалення ресурсу модуля Properties з неіснуючим ID (наприклад, `00000000-0000-0000-0000-000000000000` або `999999`).
3. Перевірити статус-код відповіді.

**Очікуваний результат:**

- Статус: `404 Not Found`
- Body містить повідомлення про те, що ресурс не знайдено.
