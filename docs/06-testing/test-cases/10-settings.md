## 10. Settings (TC-SET-001 — TC-SET-020, TC-SET-U-001 — TC-SET-U-009)

---

### [ ] TC-SET-001: Зміна аватара

| Поле         | Значення                 |
| ------------ | ------------------------ |
| **US**       | US-117                   |
| **Issue**    | #42                      |
| **TS**       | TS-USER-01               |
| **Метод**    | Black Box (Postman)      |
| **Техніка**  | Equivalence Partitioning |
| **Priority** | P1                       |

**Кроки:**

1. Увійти в систему
2. `POST /users/avatar` з multipart/form-data файлом зображення
3. Перевірити статус-код
4. `GET /users/me` — перевірити що avatarUrl оновлено

**Очікуваний результат:**

- Статус: `200 OK`
- Аватар завантажено, avatarUrl оновлено

---

### [ ] TC-SET-002: Зміна нікнейму

| Поле         | Значення                 |
| ------------ | ------------------------ |
| **US**       | US-117                   |
| **Issue**    | #42                      |
| **TS**       | TS-USER-01               |
| **Метод**    | Black Box (Postman)      |
| **Техніка**  | Equivalence Partitioning |
| **Priority** | P1                       |

**Кроки:**

1. `PATCH /users/me` з тілом: `{ "username": "newnickname" }`
2. Перевірити що нікнейм оновлено

**Очікуваний результат:**

- Статус: `200 OK`
- Нікнейм змінено

---

### [ ] TC-SET-003: Зміна email — запит

| Поле         | Значення                 |
| ------------ | ------------------------ |
| **US**       | US-118                   |
| **Issue**    | #43                      |
| **TS**       | TS-USER-01               |
| **Метод**    | Black Box (Postman)      |
| **Техніка**  | Equivalence Partitioning |
| **Priority** | P1                       |

**Кроки:**

1. `POST /users/change-email` з тілом: `{ "newEmail": "new@example.com" }`
2. Перевірити статус-код
3. Перевірити що лист верифікації надіслано

**Очікуваний результат:**

- Статус: `200 OK`
- Лист верифікації надіслано на новий email

---

### [ ] TC-SET-004: Зміна email — підтвердження

| Поле         | Значення            |
| ------------ | ------------------- |
| **US**       | US-118              |
| **Issue**    | #43                 |
| **TS**       | TS-USER-01          |
| **Метод**    | Black Box (Postman) |
| **Техніка**  | State Transition    |
| **Priority** | P1                  |

**Кроки:**

1. Виконати TC-SET-003
2. Отримати токен верифікації
3. `POST /users/verify-email` з токеном
4. Перевірити що email змінено

**Очікуваний результат:**

- Статус: `200 OK`
- Email змінено на новий, старий інвалідовано

---

### [ ] TC-SET-005: Зміна email — дублікат

| Поле         | Значення                             |
| ------------ | ------------------------------------ |
| **US**       | US-118                               |
| **Issue**    | #43                                  |
| **TS**       | TS-USER-01                           |
| **Метод**    | Black Box (Postman)                  |
| **Техніка**  | Equivalence Partitioning (duplicate) |
| **Priority** | P1                                   |

**Кроки:**

1. Спробувати змінити email на існуючий в системі
2. Перевірити статус-код

**Очікуваний результат:**

- Статус: `409 Conflict`

---

### [ ] TC-SET-006: Зміна пароля — валідний запит

| Поле         | Значення                 |
| ------------ | ------------------------ |
| **US**       | US-119                   |
| **Issue**    | #44                      |
| **TS**       | TS-USER-03               |
| **Метод**    | Black Box (Postman)      |
| **Техніка**  | Equivalence Partitioning |
| **Priority** | P1                       |

**Кроки:**

1. `POST /users/change-password` з тілом:
   ```json
   { "currentPassword": "OldPass123!", "newPassword": "NewPass456!" }
   ```
2. Перевірити статус-код
3. Спробувати увійти з новим паролем

**Очікуваний результат:**

- Статус: `200 OK`
- Вхід з новим паролем успішний

---

### [ ] TC-SET-007: Зміна пароля — інвалідація інших сесій

| Поле         | Значення            |
| ------------ | ------------------- |
| **US**       | US-119              |
| **Issue**    | #44                 |
| **TS**       | TS-USER-03          |
| **Метод**    | Black Box (Postman) |
| **Техніка**  | State Transition    |
| **Priority** | P1                  |

**Кроки:**

1. Увійти з двох пристроїв
2. Змінити пароль з першого пристрою
3. Спробувати виконати запит з другого пристрою (старий токен)
4. Перевірити статус-код

**Очікуваний результат:**

- Статус: `401 Unauthorized`
- Всі інші сесії інвалідовані

---

### [ ] TC-SET-008: Зміна пароля — неправильний поточний пароль

| Поле         | Значення            |
| ------------ | ------------------- |
| **US**       | US-119              |
| **Issue**    | #44                 |
| **TS**       | TS-USER-03          |
| **Метод**    | Black Box (Postman) |
| **Техніка**  | Error Guessing      |
| **Priority** | P1                  |

**Кроки:**

1. `POST /users/change-password` з неправильним `currentPassword`
2. Перевірити статус-код

**Очікуваний результат:**

- Статус: `401 Unauthorized`
- Пароль НЕ змінено

---

### [ ] TC-SET-009: Перемикання теми — Темна/Світла

| Поле         | Значення            |
| ------------ | ------------------- |
| **US**       | US-120              |
| **Issue**    | #93                 |
| **TS**       | —                   |
| **Метод**    | Manual (Browser)    |
| **Техніка**  | Exploratory Testing |
| **Priority** | P2                  |

**Кроки:**

1. Відкрити Settings → Appearance
2. Перемкнути тему на "Dark"
3. Перевірити що тема застосована без перезавантаження

**Очікуваний результат:**

- Тема змінена миттєво, сторінка не перезавантажується

---

### [ ] TC-SET-010: Перемикання теми — Системна

| Поле         | Значення            |
| ------------ | ------------------- |
| **US**       | US-120              |
| **Issue**    | #93                 |
| **TS**       | —                   |
| **Метод**    | Manual (Browser)    |
| **Техніка**  | Exploratory Testing |
| **Priority** | P2                  |

**Кроки:**

1. Обрати тему "System"
2. Змінити тему OS на Dark
3. Перевірити що інтерфейс змінив тему

**Очікуваний результат:**

- Інтерфейс слідує за темою OS

---

### [ ] TC-SET-011: Перемикання мови — Українська/Англійська

| Поле         | Значення            |
| ------------ | ------------------- |
| **US**       | US-121              |
| **Issue**    | #94                 |
| **TS**       | —                   |
| **Метод**    | Manual (Browser)    |
| **Техніка**  | Exploratory Testing |
| **Priority** | P2                  |

**Кроки:**

1. Settings → Language → English
2. Перевірити що всі елементи UI перекладені

**Очікуваний результат:**

- Всі тексти перекладені на англійську без перезавантаження

---

### [ ] TC-SET-012: Перемикання мови — збереження

| Поле         | Значення         |
| ------------ | ---------------- |
| **US**       | US-121           |
| **Issue**    | #94              |
| **TS**       | —                |
| **Метод**    | Manual (Browser) |
| **Техніка**  | State Transition |
| **Priority** | P2               |

**Кроки:**

1. Змінити мову на English
2. Оновити сторінку
3. Перевірити що мова залишилася English

**Очікуваний результат:**

- Мова збереглася між сесіями

---

### [ ] TC-SET-013: Формат дати — DD/MM/YYYY

| Поле         | Значення            |
| ------------ | ------------------- |
| **US**       | US-122              |
| **Issue**    | #95                 |
| **TS**       | —                   |
| **Метод**    | Manual (Browser)    |
| **Техніка**  | Exploratory Testing |
| **Priority** | P2                  |

**Кроки:**

1. Settings → Workspace → Date Format → DD/MM/YYYY
2. Перевірити відображення дат в table view

**Очікуваний результат:**

- Дати відображаються як `24/05/2026`

---

### [ ] TC-SET-014: Часовий пояс — auto-визначення

| Поле         | Значення            |
| ------------ | ------------------- |
| **US**       | US-122              |
| **Issue**    | #95                 |
| **TS**       | —                   |
| **Метод**    | Manual (Browser)    |
| **Техніка**  | Exploratory Testing |
| **Priority** | P2                  |

**Кроки:**

1. Зареєструвати нового користувача
2. Перевірити що часовий пояс визначено з браузера
3. Settings → перевірити що timezone встановлено

**Очікуваний результат:**

- Часовий пояс авто-визначено (наприклад Europe/Kyiv)

---

### [ ] TC-SET-015: Іконки за замовчуванням — налаштування

| Поле         | Значення            |
| ------------ | ------------------- |
| **US**       | US-123              |
| **Issue**    | #96                 |
| **TS**       | —                   |
| **Метод**    | Manual (Browser)    |
| **Техніка**  | Exploratory Testing |
| **Priority** | P2                  |

**Кроки:**

1. Settings → Default Icons
2. Встановити іконку за замовчуванням для Sections
3. Створити нову секцію — перевірити іконку

**Очікуваний результат:**

- Нова секція має встановлену іконку за замовчуванням
- Існуючі секції НЕ змінені

---

### [ ] TC-SET-016: Іконки за замовчуванням — існуючі елементи не змінюються

| Поле         | Значення                 |
| ------------ | ------------------------ |
| **US**       | US-123                   |
| **Issue**    | #96                      |
| **TS**       | —                        |
| **Метод**    | Manual (Browser)         |
| **Техніка**  | Equivalence Partitioning |
| **Priority** | P2                       |

**Кроки:**

1. Змінити іконку за замовчуванням
2. Перевірити що існуючі елементи зберегли свої іконки

**Очікуваний результат:**

- Існуючі елементи НЕ змінені

---

### [ ] TC-SET-017: Перегляд активних сесій — список

| Поле         | Значення            |
| ------------ | ------------------- |
| **US**       | US-124              |
| **Issue**    | #97                 |
| **TS**       | TS-USER-01          |
| **Метод**    | Manual (Browser)    |
| **Техніка**  | Exploratory Testing |
| **Priority** | P2                  |

**Кроки:**

1. Settings → Security → Active Sessions
2. Перевірити що відображається список з пристроєм, IP, датою
3. Перевірити що поточна сесія позначена

**Очікуваний результат:**

- Список сесій з усіма полями, поточна позначена

---

### [ ] TC-SET-018: Завершення конкретної сесії

| Поле         | Значення            |
| ------------ | ------------------- |
| **US**       | US-124              |
| **Issue**    | #97                 |
| **TS**       | TS-USER-01          |
| **Метод**    | Black Box (Postman) |
| **Техніка**  | State Transition    |
| **Priority** | P2                  |

**Кроки:**

1. Увійти з двох пристроїв
2. `DELETE /auth/sessions/:sessionId` (ID сесії іншого пристрою)
3. Перевірити що інша сесія завершена

**Очікуваний результат:**

- Цільова сесія завершена, refresh token інвалідовано

---

### [ ] TC-SET-019: Перемикання теми інтерфейсу (Світла/Темна) (Playwright UI E2E)

| Поле         | Значення            |
| ------------ | ------------------- |
| **US**       | US-120              |
| **Issue**    | #93                 |
| **TS**       | —                   |
| **Метод**    | E2E UI (Playwright) |
| **Техніка**  | State Transition    |
| **Priority** | P2                  |

**Кроки:**

1. Увійти в систему та перейти до розділу "Налаштування" (Settings)
2. Обрати підрозділ "Зовнішній вигляд" (Appearance)
3. Натиснути на перемикач теми та вибрати `"Темна" (Dark)`
4. Перевірити, що на тег `<html>` або `<body>` додано CSS-клас `dark` (або відповідний атрибут темної теми)
5. Перемкнути тему на `"Світла" (Light)` та перевірити зникнення класу `dark`

**Очікуваний результат:**

- Тема інтерфейсу перемикається миттєво та без перезавантаження сторінки
- Класи стилів темної теми застосовуються до всього інтерфейсу системи

---

### [ ] TC-SET-020: Оновлення відображуваного імені профілю через UI (Playwright)

| Поле         | Значення                              |
| ------------ | ------------------------------------- |
| **US**       | US-117                                |
| **Issue**    | —                                     |
| **TS**       | TS-SET-01                             |
| **Метод**    | E2E UI (Playwright)                   |
| **Техніка**  | Equivalence Partitioning (valid path) |
| **Priority** | P1                                    |

**Кроки:**

1. Увійти в систему та перейти на сторінку Profile (`/profile`)
2. Знайти поле відображуваного імені (Display Name / Username)
3. Очистити поточне значення та ввести нове ім'я `"Оновлений Профіль"`
4. Зберегти зміни натисканням кнопки Save
5. Перевірити оновлене ім'я у навігаційній кнопці header

**Очікуваний результат:**

- Нове ім'я відображається у header додатку без перезавантаження сторінки
- Зміни зберігаються після оновлення сторінки

---

## Unit Tests — Settings (TC-SET-U-001 — TC-SET-U-009)

### [ ] TC-SET-U-001: SettingsService.updateProfile — оновлення нікнейму та валідація аватара

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-117             |
| **Issue**    | #42                |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати `SettingsService.updateProfile()` для оновлення нікнейму.
2. Спробувати завантажити файл аватару формату `SVG` або розміром `6 MB`.
3. Завантажити валідний аватар (`PNG`, `2 MB`).

**Очікуваний результат:**

- Нікнейм оновлюється без помилок.
- Невалідний формат/розмір файлу аватару повертає `BadRequestException` (дозволені лише зображення до 5 MB).
- Валідний аватар успішно завантажується, створюється посилання `avatarUrl`.

---

### [ ] TC-SET-U-002: SettingsService.requestEmailChange — надсилання коду підтвердження на нову адресу

| Поле         | Значення                 |
| ------------ | ------------------------ |
| **US**       | US-118                   |
| **Issue**    | #43                      |
| **TS**       | —                        |
| **Метод**    | Unit (Jest)              |
| **Техніка**  | Equivalence Partitioning |
| **Priority** | P1                       |

**Кроки:**

1. Викликати `SettingsService.requestEmailChange()` з адресою, яка вже зареєстрована в системі.
2. Викликати `SettingsService.requestEmailChange()` з новою унікальною адресою.

**Очікуваний результат:**

- Використання дублікату email викликає `ConflictException`.
- Нова адреса ініціює створення токену підтвердження та надсилання листа верифікації на нову скриньку. Поточний email користувача не змінюється до моменту підтвердження.

---

### [ ] TC-SET-U-003: SettingsService.changePassword — валідація старого пароля та анулювання активних сесій

| Поле         | Значення         |
| ------------ | ---------------- |
| **US**       | US-119           |
| **Issue**    | #44              |
| **TS**       | —                |
| **Метод**    | Unit (Jest)      |
| **Техніка**  | State Transition |
| **Priority** | P1               |

**Кроки:**

1. Викликати `SettingsService.changePassword()` з неправильним поточним паролем.
2. Викликати `SettingsService.changePassword()` з правильним поточним паролем та новим паролем.
3. Перевірити видалення всіх `RefreshToken` користувача, окрім поточного, або повну інвалідацію всіх сесій.

**Очікуваний результат:**

- Невірний старий пароль повертає `UnauthorizedException` або `BadRequestException`.
- Успішна зміна пароля оновлює хеш в БД та анулює всі інші активні сесії користувача.

---

### [ ] TC-SET-U-004: SessionService.revokeAllExceptCurrent — завершення всіх сесій крім поточної

| Поле         | Значення         |
| ------------ | ---------------- |
| **US**       | US-124           |
| **Issue**    | #97              |
| **TS**       | —                |
| **Метод**    | Unit (Jest)      |
| **Техніка**  | State Transition |
| **Priority** | P2               |

**Кроки:**

1. Користувач має 3 активні сесії в базі даних.
2. Викликати `SessionService.revokeAllExceptCurrent()` передавши ID поточної сесії.
3. Перевірити статус сесій у базі даних.

**Очікуваний результат:**

- Інші 2 сесії видаляються з бази даних (refresh токени стають недійсними).
- Поточна сесія залишається активною.

---

### [ ] TC-SET-U-005: WorkspaceSettingsService.updateTimezone — валідація IANA часового поясу та перерахунок відображення

| Поле         | Значення                 |
| ------------ | ------------------------ |
| **US**       | US-121                   |
| **Issue**    | #94                      |
| **TS**       | —                        |
| **Метод**    | Unit (Jest)              |
| **Техніка**  | Equivalence Partitioning |
| **Priority** | P2                       |

**Кроки:**

1. Спробувати встановити невалідний часовий пояс (`"Kyiv/Ukraine"`).
2. Встановити валідний пояс (`"Europe/Kyiv"`).
3. Перевірити, що збережена в UTC дата відображається відповідно до зміщеного часу нового поясу.

**Очікуваний результат:**

- Невалідний пояс відхиляється помилкою валідації.
- Валідний пояс зберігається успішно; функції відображення дати у системі застосовують часовий зсув Europe/Kyiv.

---

### [ ] TC-SET-U-006: WorkspaceSettingsService.updateFormat — вибір формату дати, часу (12/24) та першого дня тижня

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-121             |
| **Issue**    | #94                |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P2                 |

**Кроки:**

1. Змінити формат часу на `12-годинний` (AM/PM) та перший день тижня на `Sunday`.
2. Перевірити відображення дати/часу на тестовому значенні.

**Очікуваний результат:**

- Параметри зберігаються успішно; дати та календарі адаптуються под неділю як перший день тижня, а часові мітки відображаються у форматі 12h.

---

### [ ] TC-SET-U-007: SectionSettingsService.updateDefaults — збереження іконки та кольору за замовчуванням для нових секцій

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-122             |
| **Issue**    | #95                |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P3                 |

**Кроки:**

1. Викликати `SectionSettingsService.updateDefaults()` з іконкою `"📁"` та кольором `"blue"`.
2. Створити нову секцію без вказання іконки та кольору.

**Очікуваний результат:**

- Налаштування за замовчуванням збережені.
- Створена секція автоматично отримує іконку `"📁"` та колір `"blue"`.

---

### [ ] TC-SET-U-008: DatabaseSettingsService.updateDefaults — конфігурація іконки за замовчуванням для нових баз

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-123             |
| **Issue**    | #96                |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P3                 |

**Кроки:**

1. Викликати `DatabaseSettingsService.updateDefaults()` з іконкою `"📊"`.
2. Створити нову базу даних без вказання іконки.

**Очікуваний результат:**

- База даних успішно створюється та за замовчуванням отримує іконку `"📊"`.

---

### [ ] TC-SET-U-009: RecordSettingsService.updateDefaults — налаштування іконок та поведінки автовідкриття

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-123             |
| **Issue**    | #96                |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P3                 |

**Кроки:**

1. Оновити дефолтні налаштування записів: іконка за замовчуванням `"📝"`, авто-відкриття запису (`autoOpen: true`).
2. Створити новий запис. Перевірити застосовану іконку та прапорець авто-відкриття у відповіді API.

**Очікуваний результат:**

- Створений запис має іконку `"📝"`.
- Прапорець `autoOpen: true` повертається клієнту для ініціації відкриття модального вікна.
