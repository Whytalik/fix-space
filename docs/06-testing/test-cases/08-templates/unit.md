# Unit Tests: Templates

### [ ] TC-TMPL-U-001: TemplateService.resetSystemTemplate — відновлення початкового стану системного шаблону

| Поле         | Значення         |
| ------------ | ---------------- |
| **US**       | US-005           |
| **Issue**    | #20              |
| **TS**       | —                |
| **Метод**    | Unit (Jest)      |
| **Техніка**  | State Transition |
| **Priority** | P2               |

**Кроки:**

1. Отримати системний шаблон у модифікованому стані (змінено назву, іконку, значення властивостей та контентну область).
2. Викликати `TemplateService.resetSystemTemplate()`.
3. Перевірити стан шаблону після скидання.

**Очікуваний результат:**

- Системний шаблон повертається до значень, які він мав при ініціалізації простору.
- Кастомні шаблони не можуть бути скинуті цим методом (повертається помилка `BadRequestException`).

---

---

### [ ] TC-TMPL-U-002: TemplateService.duplicate — копіювання властивостей та контенту

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-061             |
| **Issue**    | #89                |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P2                 |

**Кроки:**

1. Викликати `TemplateService.duplicate()` для існуючого шаблону.
2. Перевірити значення властивостей та контентної області дубльованого шаблону.

**Очікуваний результат:**

- Створюється новий шаблон з назвою за патерном `"[Назва шаблону] (Copy)"`.
- Усі властивості, іконка, опис та контентна область повністю скопійовані із вихідного шаблону.

---

---

### [ ] TC-TMPL-U-003: TemplateService.setDefault — встановлення дефолтного шаблону та зняття попереднього

| Поле         | Значення         |
| ------------ | ---------------- |
| **US**       | US-013           |
| **Issue**    | #21              |
| **TS**       | —                |
| **Метод**    | Unit (Jest)      |
| **Техніка**  | State Transition |
| **Priority** | P1               |

**Кроки:**

1. Встановити шаблон А як дефолтний для бази даних.
2. Встановити шаблон Б як дефолтний для тієї ж бази даних.
3. Перевірити прапорець `isDefault` обох шаблонів у БД.

**Очікуваний результат:**

- Шаблон Б стає дефолтним (`isDefault: true`).
- Для шаблону А прапорець автоматично скидається на `false` (тільки один шаблон у базі даних може бути за замовчуванням).

---

---

### [ ] TC-TMPL-U-004: NamePatternParser.parse — токени дати та часу (today, month, month_num, year, quarter)

| Поле         | Значення                 |
| ------------ | ------------------------ |
| **US**       | US-005                   |
| **Issue**    | #88                      |
| **TS**       | —                        |
| **Метод**    | Unit (Jest)              |
| **Техніка**  | Equivalence Partitioning |
| **Priority** | P1                       |

**Кроки:**

1. Викликати `NamePatternParser.parse()` з патерном `"Log {{today}} - {{month}} - {{month_num}} - {{year}} - Q{{quarter}}"` для дати `04.06.2026`.
2. Перевірити отриманий результат.

**Очікуваний результат:**

- Результат відповідає: `"Log 04.06.2026 - June - 06 - 2026 - Q2"`.

---

---

### [ ] TC-TMPL-U-005: NamePatternParser.parse — токен count (кількість записів + 1)

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-005                  |
| **Issue**    | #88                     |
| **TS**       | —                       |
| **Метод**    | Unit (Jest)             |
| **Техніка**  | Boundary Value Analysis |
| **Priority** | P1                      |

**Кроки:**

1. В базі даних є 3 активних записи та 2 м'яко видалених.
2. Викликати `NamePatternParser.parse()` з патерном `"Record #{{count}}"`.
3. Перевірити згенероване ім'я.

**Очікуваний результат:**

- Оскільки активних записів 3, то `count` дорівнює `3 + 1 = 4`.
- Результат: `"Record #4"`. М'яко видалені записи ігноруються.

---

---

### [ ] TC-TMPL-U-006: NamePatternParser.parse — токен count з фільтром властивості (count:PropertyName=value)

| Поле         | Значення                 |
| ------------ | ------------------------ |
| **US**       | US-005                   |
| **Issue**    | #88                      |
| **TS**       | —                        |
| **Метод**    | Unit (Jest)              |
| **Техніка**  | Equivalence Partitioning |
| **Priority** | P2                       |

**Кроки:**

1. В базі даних є записи з властивістю `Period Type`: два записи мають значення `Weekly`, один запис має значення `Daily`.
2. Викликати `NamePatternParser.parse()` з патерном `"Weekly Review #{{count:Period Type=Weekly}}"`.

**Очікуваний результат:**

- Токен обчислює кількість записів, де `Period Type` дорівнює `Weekly` (це 2 записи).
- Результат: `"Weekly Review #3"`.

---

---

### [ ] TC-TMPL-U-007: TemplateService.createFromRecord — збереження поточного стану запису як нового шаблону

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | #87                |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P2                 |

**Кроки:**

1. Створити запис у базі даних із заповненими властивостями та структурованою контентною областю.
2. Викликати `TemplateService.createFromRecord()` передавши ID запису та ім'я нового шаблону.
3. Перевірити створений шаблон.

**Очікуваний результат:**

- Створюється кастомний шаблон для даної бази даних.
- Шаблон містить аналогічні значення властивостей та таку ж саму структуру контентної області, що й вихідний запис.

---

---

---

### [ ] TC-TMPL-U-008: TemplateService — Відкат транзакції при виникненні помилки

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Налаштувати мок репозиторію або Prisma клієнта на успішне виконання першої частини транзакції (наприклад, створення запису).
2. Налаштувати мок репозиторію так, щоб наступний крок транзакції або супутня дія викликали виняток/помилку.
3. Викликати відповідний метод сервісу/usecase в транзакції.
4. Перевірити, що всі зміни, виконані в межах транзакції, відкочуються і помилка прокидається вище.

**Очікуваний результат:**

- Транзакція відміняється (rollback).
- Жодні проміжні зміни не зберігаються в базі даних.
- Метод прокидає відповідну помилку.

---

---

### [ ] TC-TMPL-U-009: TemplateService — Обмеження доступу до ресурсу, який не належить користувачу

| Поле         | Значення        |
| ------------ | --------------- |
| **US**       | US-005          |
| **Issue**    | —               |
| **TS**       | —               |
| **Метод**    | Unit (Jest)     |
| **Техніка**  | Branch Coverage |
| **Priority** | P1              |

**Кроки:**

1. Налаштувати мок репозиторію для повернення ресурсу, де `ownerId` відрізняється від ID поточного користувача.
2. Викликати метод сервісу для доступу або модифікації цього ресурсу від імені поточного користувача.
3. Перевірити, що метод викидає `ForbiddenException`.

**Очікуваний результат:**

- Метод повертає `ForbiddenException` та не виконує операцію.

---

---

### [ ] TC-TMPL-U-010: TemplateService — Обробка помилки дублювання унікальних полів

| Поле         | Значення       |
| ------------ | -------------- |
| **US**       | US-005         |
| **Issue**    | —              |
| **TS**       | —              |
| **Метод**    | Unit (Jest)    |
| **Техніка**  | Error Guessing |
| **Priority** | P1             |

**Кроки:**

1. Налаштувати мок репозиторію для імітації порушення унікального обмеження у БД (наприклад, унікального імені чи email).
2. Викликати відповідний метод створення або оновлення сервісу.
3. Перевірити, що метод перехоплює помилку репозиторію та викидає `ConflictException`.

**Очікуваний результат:**

- Метод повертає `ConflictException` із повідомленням про помилку дублювання.
