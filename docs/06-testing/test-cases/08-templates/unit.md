# Unit Tests: Templates

### [x] TC-TMPL-U-001: TemplateService.resetSystemTemplate — відновлення початкового стану системного шаблону

| Поле         | Значення         |
| ------------ | ---------------- |
| **US**       | US-005           |
| **Issue**    | #20              |
| **TS**       | —                |
| **Метод**    | Unit (Jest)      |
| **Техніка**  | State Transition |
| **Priority** | P2               |

**Кроки:**

1. Отримати системний шаблон у модифікованому стані (змінено назву, іконку, значення властивостей та контентну область).
2. Викликати `TemplateService.resetSystemTemplate()`.
3. Перевірити стан шаблону після скидання.

**Очікуваний результат:**

- Системний шаблон повертається до значень, які він мав при ініціалізації простору.
- Кастомні шаблони не можуть бути скинуті цим методом (повертається помилка `BadRequestException`).

---

---

### [x] TC-TMPL-U-002: TemplateService.duplicate — копіювання властивостей та контенту

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-061             |
| **Issue**    | #89                |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P2                 |

**Кроки:**

1. Викликати `TemplateService.duplicate()` для існуючого шаблону.
2. Перевірити значення властивостей та контентної області дубльованого шаблону.

**Очікуваний результат:**

- Створюється новий шаблон з назвою за патерном `"[Назва шаблону] (Copy)"`.
- Усі властивості, іконка, опис та контентна область повністю скопійовані із вихідного шаблону.

---

---

### [x] TC-TMPL-U-003: TemplateService.setDefault — встановлення дефолтного шаблону та зняття попереднього

| Поле         | Значення         |
| ------------ | ---------------- |
| **US**       | US-013           |
| **Issue**    | #21              |
| **TS**       | —                |
| **Метод**    | Unit (Jest)      |
| **Техніка**  | State Transition |
| **Priority** | P1               |

**Кроки:**

1. Встановити шаблон А як дефолтний для бази даних.
2. Встановити шаблон Б як дефолтний для тієї ж бази даних.
3. Перевірити прапорець `isDefault` обох шаблонів у БД.

**Очікуваний результат:**

- Шаблон Б стає дефолтним (`isDefault: true`).
- Для шаблону А прапорець автоматично скидається на `false` (тільки один шаблон у базі даних може бути за замовчуванням).

---

---

### [x] TC-TMPL-U-004: NamePatternParser.parse — токени дати та часу (today, month, month_num, year, quarter)

| Поле         | Значення                 |
| ------------ | ------------------------ |
| **US**       | US-005                   |
| **Issue**    | #88                      |
| **TS**       | —                        |
| **Метод**    | Unit (Jest)              |
| **Техніка**  | Equivalence Partitioning |
| **Priority** | P1                       |

**Кроки:**

1. Викликати `NamePatternParser.parse()` з патерном `"Log {{today}} - {{month}} - {{month_num}} - {{year}} - Q{{quarter}}"` для дати `04.06.2026`.
2. Перевірити отриманий результат.

**Очікуваний результат:**

- Результат відповідає: `"Log 04.06.2026 - 06 - 06 - 2026 - Q2"`.

---

---

### [x] TC-TMPL-U-005: NamePatternParser.parse — токен count (кількість записів + 1)

| Поле         | Значення                |
| ------------ | ----------------------- |
| **US**       | US-005                  |
| **Issue**    | #88                     |
| **TS**       | —                       |
| **Метод**    | Unit (Jest)             |
| **Техніка**  | Boundary Value Analysis |
| **Priority** | P1                      |

**Кроки:**

1. В базі даних є 3 активних записи та 2 м'яко видалених.
2. Викликати `NamePatternParser.parse()` з патерном `"Record #{{count}}"`.
3. Перевірити згенероване ім'я.

**Очікуваний результат:**

- Оскільки активних записів 3, то `count` дорівнює `3 + 1 = 4`.
- Результат: `"Record #4"`. М'яко видалені записи ігноруються.

---

---

### [x] TC-TMPL-U-006: NamePatternParser.parse — токен count з фільтром властивості (count:PropertyName=value)

| Поле         | Значення                 |
| ------------ | ------------------------ |
| **US**       | US-005                   |
| **Issue**    | #88                      |
| **TS**       | —                        |
| **Метод**    | Unit (Jest)              |
| **Техніка**  | Equivalence Partitioning |
| **Priority** | P2                       |

**Кроки:**

1. В базі даних є записи з властивістю `Period Type`: два записи мають значення `Weekly`, один запис має значення `Daily`.
2. Викликати `NamePatternParser.parse()` з патерном `"Weekly Review #{{count:Period Type=Weekly}}"`.

**Очікуваний результат:**

- Токен обчислює кількість записів, де `Period Type` дорівнює `Weekly` (це 2 записи).
- Результат: `"Weekly Review #3"`.

---

---

### [x] TC-TMPL-U-007: TemplateService.createFromRecord — збереження поточного стану запису як нового шаблону

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | #87                |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P2                 |

**Кроки:**

1. Створити запис у базі даних із заповненими властивостями та структурованою контентною областю.
2. Викликати `TemplateService.createFromRecord()` передавши ID запису та ім'я нового шаблону.
3. Перевірити створений шаблон.

**Очікуваний результат:**

- Створюється кастомний шаблон для даної бази даних.
- Шаблон містить аналогічні значення властивостей та таку ж саму структуру контентної області, що й вихідний запис.

---

---

---

### [x] TC-TMPL-U-008: TemplateService — Відкат транзакції при виникненні помилки

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Налаштувати мок репозиторію або Prisma клієнта на успішне виконання першої частини транзакції (наприклад, створення запису).
2. Налаштувати мок репозиторію так, щоб наступний крок транзакції або супутня дія викликали виняток/помилку.
3. Викликати відповідний метод сервісу/usecase в транзакції.
4. Перевірити, що всі зміни, виконані в межах транзакції, відкочуються і помилка прокидається вище.

**Очікуваний результат:**

- Транзакція відміняється (rollback).
- Жодні проміжні зміни не зберігаються в базі даних.
- Метод прокидає відповідну помилку.

---

---

### [x] TC-TMPL-U-009: TemplateService — Обмеження доступу до ресурсу, який не належить користувачу

| Поле         | Значення        |
| ------------ | --------------- |
| **US**       | US-005          |
| **Issue**    | —               |
| **TS**       | —               |
| **Метод**    | Unit (Jest)     |
| **Техніка**  | Branch Coverage |
| **Priority** | P1              |

**Кроки:**

1. Налаштувати мок репозиторію для повернення ресурсу, де `ownerId` відрізняється від ID поточного користувача.
2. Викликати метод сервісу для доступу або модифікації цього ресурсу від імені поточного користувача.
3. Перевірити, що метод викидає `ForbiddenException`.

**Очікуваний результат:**

- Метод повертає `ForbiddenException` та не виконує операцію.

---

---

### [x] TC-TMPL-U-010: TemplateService — Обробка помилки дублювання унікальних полів

| Поле         | Значення       |
| ------------ | -------------- |
| **US**       | US-005         |
| **Issue**    | —              |
| **TS**       | —              |
| **Метод**    | Unit (Jest)    |
| **Техніка**  | Error Guessing |
| **Priority** | P1             |

**Кроки:**

1. Налаштувати мок репозиторію для імітації порушення унікального обмеження у БД (наприклад, унікального імені чи email).
2. Викликати відповідний метод створення або оновлення сервісу.
3. Перевірити, що метод перехоплює помилку репозиторію та викидає `ConflictException`.

**Очікуваний результат:**

- Метод повертає `ConflictException` із повідомленням про помилку дублювання.

---

### [x] TC-TMPL-U-011: should throw NotFoundException when template not found

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що throw NotFoundException коли template not found.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-012: should throw NotFoundException when property not found

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що throw NotFoundException коли property not found.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-013: should throw ConflictException when property database mismatch

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що throw ConflictException коли property database mismatch.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-014: should throw BadRequestException when value is invalid

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що throw BadRequestException коли value is invalid.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-015: should create template property value successfully

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що create template property value successfully.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-016: should return list of template property values

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що return list of template property values.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-017: should throw NotFoundException when value not found

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що throw NotFoundException коли value not found.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-018: should return value when found

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що return value коли found.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-019: should throw NotFoundException when updating non-existent value

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що throw NotFoundException коли updating non-existent value.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-020: should throw BadRequestException when updated value is invalid

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що throw BadRequestException коли updated value is invalid.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-021: should update value successfully

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що update value successfully.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-022: should throw NotFoundException when deleting non-existent value

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що throw NotFoundException коли deleting non-existent value.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-023: should remove value successfully

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що remove value successfully.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-024: should call service.create with correct arguments

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що call service.create with correct arguments.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-025: should call service.findAll with correct arguments

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що call service.findAll with correct arguments.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-026: should call service.findOne with correct arguments

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що call service.findOne with correct arguments.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-027: should call service.update with correct arguments

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що call service.update with correct arguments.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-028: should call service.remove with correct arguments

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що call service.remove with correct arguments.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-029: should call service.create with correct arguments

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що call service.create with correct arguments.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-030: should call service.findAll with correct arguments

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що call service.findAll with correct arguments.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-031: should call service.findOne with correct arguments

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що call service.findOne with correct arguments.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-032: should call service.update with correct arguments

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що call service.update with correct arguments.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-033: should call duplicateUseCase.execute with correct arguments

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що call duplicateUseCase.execute with correct arguments.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-034: should call service.remove with correct arguments

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що call service.remove with correct arguments.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-035: should call service.reset with correct arguments

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що call service.reset with correct arguments.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-036: should call service.uploadImage with correct arguments

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що call service.uploadImage with correct arguments.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-037: should call prisma.findUnique

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що call prisma.findUnique.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-038: should call prisma.findMany

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що call prisma.findMany.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-039: should call prisma.upsert

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що call prisma.upsert.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-040: should call prisma.update

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що call prisma.update.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-041: should call prisma.delete

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що call prisma.delete.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-042: should call prisma.findFirst

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що call prisma.findFirst.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-043: should call prisma.findUnique

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що call prisma.findUnique.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-044: should call prisma.findUnique with values include

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що call prisma.findUnique with values include.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-045: should call prisma.findMany

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що call prisma.findMany.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-046: should call prisma.count

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що call prisma.count.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-047: should return name with (Copy) if name does not exist

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що return name with (Copy) if name does not exist.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-048: should increment counter if name exists

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що increment counter if name exists.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-049: should call prisma.create

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що call prisma.create.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-050: should call prisma.findUniqueOrThrow

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що call prisma.findUniqueOrThrow.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-051: should call prisma.update

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що call prisma.update.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-052: should call prisma.updateMany

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що call prisma.updateMany.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-053: should call prisma.findFirst with asc position sorting

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що call prisma.findFirst with asc position sorting.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-054: should call prisma.findFirst with isDefault: true

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що call prisma.findFirst with isDefault: true.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-055: should call prisma.findFirst with name and db

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що call prisma.findFirst with name and db.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-056: should call prisma.delete

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що call prisma.delete.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-057: should throw NotFoundException if database not found

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що throw NotFoundException if database not found.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-058: should set isDefault to true for first template

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що set isDefault to true for first template.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-059: should reset other defaults when creating new default

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що reset other defaults коли creating new default.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-060: should return all templates in database

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що return all templates in database.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-061: should throw NotFoundException if template not found

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що throw NotFoundException if template not found.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-062: should return template with values when found

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що return template with values коли found.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-063: should assign new default when old default is removed

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що assign new default коли old default is removed.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-064: should throw NotFoundException when template not found

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що throw NotFoundException коли template not found.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-065: should throw NotFoundException when deleting non-existent template

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що throw NotFoundException коли deleting non-existent template.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-066: should delete template successfully and set next default if deleted was default

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що delete template successfully and set next default if deleted was default.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-067: should throw NotFoundException when resetting non-existent template

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що throw NotFoundException коли resetting non-existent template.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-068: should reset template property values and configuration

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що reset template property values and configuration.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---

### [x] TC-TMPL-U-069: should save image and return url

| Поле         | Значення           |
| ------------ | ------------------ |
| **US**       | US-005             |
| **Issue**    | —                  |
| **TS**       | —                  |
| **Метод**    | Unit (Jest)        |
| **Техніка**  | Statement Coverage |
| **Priority** | P1                 |

**Кроки:**

1. Викликати відповідний метод із тестовим сценарієм.
2. перевірити, що save image and return url.

**Очікуваний результат:**

- Успішне виконання та відповідність очікуваному стану/помилці.

---
