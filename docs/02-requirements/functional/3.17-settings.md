[<- Назад до змісту](../functional.md)

## 3.17 Налаштування

**Налаштування** — це централізована сторінка управління параметрами облікового запису, інтерфейсу та значень за замовчуванням для об'єктів системи. Сторінка організована у вісім незалежних груп, розподілених за логічним рівнем: особистий акаунт, відображення інтерфейсу, об'єкти системи та зовнішні підключення.

**Профіль** — персональні дані облікового запису та управління його існуванням.

**Безпека** — управління засобами автентифікації та активними сесіями.

**Зовнішній вигляд** — глобальні параметри відображення інтерфейсу, незалежні від конкретного простору.

**Робочий простір** — параметри відображення даних, що застосовуються в межах усіх просторів: формат дат і часу, початок тижня. Ці налаштування впливають на те, як відображаються значення в базах даних, а не на візуальний стиль інтерфейсу.

**Секції** — значення за замовчуванням, що застосовуються при створенні нової секції.

**Бази даних** — значення за замовчуванням, що застосовуються при створенні нової бази даних.

**Записи** — значення за замовчуванням, що застосовуються при створенні нового запису.

**Інтеграції** — управління підключеннями до зовнішніх брокерських сервісів (детальна специфікація — розділ 3.12).

**Функціональні вимоги:**

Профіль:

- [x] Зміна аватару (завантаження нового зображення, видалення поточного) <!-- API/Lib ready, UI partially missing -->
- [x] Зміна нікнейму
- [x] Видалення облікового запису (потрібне підтвердження паролем; всі дані видаляються безповоротно; підтвердження надсилається на email)

Безпека:

- [x] Зміна пароля (потрібен поточний пароль; після зміни всі активні сесії анулюються)

Зовнішній вигляд:

- [x] Вибір теми оформлення: Світла / Темна / Як у системі
- [x] Вибір мови інтерфейсу: uk / en

Робочий простір:

- [x] Вибір формату відображення дат
- [x] Вибір формату відображення часу: 12-годинний / 24-годинний
- [x] Вибір першого дня тижня: понеділок / неділя
- [x] Вибір часового поясу (IANA-формат, наприклад Europe/Kyiv, America/New_York); за замовчуванням — автовизначення з браузера при реєстрації; усі дати та часові мітки відображаються у вибраному поясі, зберігаються в UTC
- [x] Перейменування та видалення наявних робочих просторів (видалення основного простору заборонено)
- [x] Зміна основного робочого простору (встановити інший простір як основний)

Секції:

- [x] Вибір іконки та кольору за замовчуванням для нових секцій

Бази даних:

- [x] Вибір іконки за замовчуванням для нових баз даних

Записи:

- [x] Вибір іконки за замовчуванням для нових записів

Інтеграції:

- [x] Перелік усіх налаштованих підключень до зовнішніх сервісів із відображенням статусу кожного
- [x] Додавання нового підключення
- [x] Перепідключення (оновлення credentials) наявного підключення
- [x] Відключення наявного підключення (з підтвердженням)
