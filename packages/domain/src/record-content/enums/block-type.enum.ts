export enum BlockType {
  // Leaf nodes (primitive components)
  TEXT = 'TEXT',
  HEADING = 'HEADING',
  IMAGE = 'IMAGE',
  DIVIDER = 'DIVIDER',

  // Container nodes
  ROW = 'ROW',
  COLUMN = 'COLUMN',
  BLOCK = 'BLOCK',
}
