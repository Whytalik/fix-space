export enum GroupField {
  PROPERTY = "property",
  CREATED_AT = "createdAt",
  UPDATED_AT = "updatedAt",
}

export enum DateGroupGranularity {
  DAY = "day",
  WEEK = "week",
  MONTH = "month",
  YEAR = "year",
}
